[pytest]
# tests/ 만 수집한다. experiments/의 pulp_test.py 등은 파일명이 pytest 기본 패턴과
# 겹치지만 테스트가 아니라 학습용 스크립트이므로 수집 대상에서 제외한다.
testpaths = tests
addopts = -v --tb=short
markers =
    slow: 실제로 파이프라인을 돌리는 느린 테스트 (재현 절차 검증)
