# 두 PC로 번갈아 작업하기 — 무엇을 옮기고 무엇을 옮기지 않는가

> 2026-08-31 정리. 회사 노트북과 개인 노트북 두 곳에서 같은 저장소를 번갈아
> 쓸 때, `.gitignore`에 걸린 것들을 어떻게 다룰지 정합니다.

**결론부터: 매번 동기화할 것은 없습니다.** 초기 1회 세팅만 하면 이후는 git
`push`/`pull`로 충분합니다. `.gitignore`에 걸린 것들은 성격이 네 가지로 갈리고,
**그중 손으로 옮겨야 하는 것은 원본 데이터와 `.env` 둘뿐입니다.**

> 이 문서는 **개발을 번갈아 하는** 경우를 다룹니다. **지금 어느 환경이 무엇을
> 수집하는지는 아래 0장이 정본입니다.** 겹쳐도 안전한 이유와 병합 명령은
> [COLLECTOR.md 11장](COLLECTOR.md#11-두-번째-pc가-휴일을-맡는다-2026-08-27-1250·1254)에
> 있습니다(그 장의 *A=평일 / B=휴일* 분담은 2026-09-03에 폐기됐습니다).

---

## 0. 지금 어느 환경이 무엇을 맡고 있나 (2026-09-03)

**이 절이 수집 분담의 정본입니다.** 수집기를 켜고 끌 때 여기를 먼저 고치십시오 —
어느 환경이 도는지 모르면 **같은 값을 두 번 받거나, 아무도 안 받습니다.**

| | **회사환경 (A)** | **집환경 (B)** |
| --- | --- | --- |
| 부르는 이름 | 회사 노트북 · 지금까지 "A PC" | 개인 노트북 · 지금까지 "B PC" |
| **재고 수집** | ✅ **활성** — 매일(휴일 포함) **24시간**(`00:00–23:50`) · 10분 (2026-09-14) | ✅ **활성** — 매일(휴일 포함) 07:00–23:00 · 10분 — ⚠️ **아직 옛 창**(아래) |
| **도로 수집** | ⏸ **일시정지** (2026-09-03) | ✅ **활성** — 매일(휴일 포함) 09/12/15/18/21시 슬롯 + 로그온 (휴일분은 2026-09-08부터 · [COLLECTOR_ROAD.md](COLLECTOR_ROAD.md) 5장) |
| 실제 켜져 있는 때 | **24시간** — 퇴근 뒤에도 덮개 닫고 전원 연결 (2026-09-14 결정 · 첫 밤샘 89/89틱, 09-15 확인) | 대략 **18:00–23:00** |
| 작업 이름 | `PBR재고수집` · `PBR도로시간수집` | 같음 |

📌 **추석 연휴(9/24~27 · 10/3~5 · 10/9~11)에도 도로 수집은 B만 맡습니다** (2026-09-15 사용자 결정) —
A의 `PBR도로시간수집`은 일시정지 그대로입니다. 연휴 도로 표본은 **B가 깨어 있는 날만** 모이므로
휴일마다 B를 켜서 로그인합니다([사람이_할_일.md](../기록/사람이_할_일.md) ①-B).

등록 명령 — **회사환경(A)은 24시간으로 바꿨습니다**(2026-09-14, 1.26.206).

```powershell
.\scripts\collector.ps1 install -Window 00:00-23:50 -IncludeHolidays
```

⚠️ **집환경(B)은 아직 `07:00-23:00`입니다.** `--status`가 쓰는 창 이력표(`collect_stock.WINDOW_HISTORY`)는
**환경을 구분하지 않아** 09-15부터 기대를 144틱으로 셉니다 — 두 환경을 합친 DB에서는 맞고 거짓 '온전'도
나지 않지만 **B 혼자서는 온전한 날이 뜨지 않습니다**('결측'은 원래 합치기 전 판정이 아닙니다). 다음에 집 PC
앞에 있을 때 **위 명령으로 같이 맞추길 권합니다** — B는 깨어 있을 때만 찍으므로 창을 넓혀 잃는 것이 없습니다.
▶ **B도 24시간으로 다시 등록하기로 했습니다**(2026-09-15 사용자 결정, 아직 안 함). 창은 작업 인자에
박혀 있어 **`install`을 다시 해야 합니다** — `git pull` 뒤 위 명령을 그대로 쓰면 기존 작업을 덮어씁니다.
`액세스가 거부되었습니다`가 나오면 관리자 PowerShell에서 다시 하십시오(도로 수집기를 B에 재등록할 때
그랬습니다, 1.26.160). 끝나면 `.\scripts\collector.ps1 status`의 창이 `00:00-23:50`인지 보고 이 표의 B 칸을 고칩니다.

### 🔴 회사환경(A)이 주말을 맡는다 (2026-09-13 결정)

**집환경(B)은 주말 휴일 재고를 혼자 못 채웁니다.** 09-12·13 실측에서 자동
수집이 **45틱 / 1틱**에 그쳤습니다. 원인은 수집기가 아니라 **Modern Standby**
입니다 — 이 기기는 화면이 꺼지면 S0 저전원 유휴로 내려가고, 그 상태에서
예약 작업이 `0x800710E0`으로 거부됩니다([사람이_할_일.md](../기록/사람이_할_일.md) ①).

게다가 **이동 중에는 원리적으로 불가능합니다** — 배터리 + 덮개 닫힘이라
S0를 피할 방법이 없습니다(레지스트리로 끄면 가방 안에서 풀가동되어 발열합니다).

▶ **그래서 회사환경(A)을 주말·공휴일에도 24시간 켜 둡니다.**
**회사에서 따라 할 절차는 바로 아래 [0-A장](#0-a-회사환경-주말-가동--회사에서-이-순서대로-2026-09-13)에 있습니다.** 설정은 이미
`-IncludeHolidays` · **24시간**(`00:00-23:50`, 2026-09-14 재등록)이라 **다시 등록할 것이 없습니다.**
켜 두기만 하면 그날부터 모입니다.

⚠️ **회사환경도 노트북입니다.** 켜 두는 것만으로는 부족할 수 있습니다 —
같은 함정에 걸리는지 **첫 주말 뒤에 반드시 확인하십시오.**

```powershell
# 회사환경에서 (월요일에 확인)
.\scripts\collector.ps1 status          # 주말 이틀의 틱 수를 본다
powercfg /a                              # S0 저전원 유휴 기기인가
```

🔴 **S0 기기라면 충전기를 꽂아 두는 것이 필수입니다.** 집환경 실측에서
*덮개 닫음 + 충전기*는 유휴 2시간 21분 동안 정상 수집됐고, *덮개 닫음 +
배터리*는 3~6분 만에 멈췄습니다.
회사환경은 **충전기만으로는 모자랐고 절전(AC) '안 함'까지** 필요해 보입니다
(2026-09-14 실측 1회 — 0-A 1단계 📌).

📌 **판정은 합친 뒤에 합니다.** A가 주말을 덮으면 B의 주말 결측은 문제가
아닙니다 — `merge_stock.py`로 합쳐야 진짜 공백이 보입니다(아래 0장 경고).

---

### 왜 재고는 둘 다 같은 창인가

**요일이나 시간으로 나누지 않습니다** (2026-09-03 사용자 결정). 양쪽 모두
매일 07:00–23:00으로 걸어 두고, **실제로 켜져 있는 동안 모이는 것이 곧 이득**입니다.

- 회사환경은 낮(08:30–17:30), 집환경은 저녁(18:00–23:00)에 켜져 있어
  **자연스럽게 하루를 나눠 덮습니다.** 창을 손으로 자를 필요가 없습니다.
- **겹쳐도 안전합니다.** `tick_of()`가 같은 격자로 반올림하므로 두 환경의 09:10은
  같은 키가 되고, `merge_stock.py`가 `INSERT OR IGNORE`로 합칩니다 —
  중복은 병합 때 걸러집니다(COLLECTOR.md 11-1장).
- 창을 좁게 자르면 **가동 시간이 바뀔 때마다 다시 등록**해야 하고, 그 사이가
  구멍으로 남습니다. 넓게 걸어 두면 켜져 있는 만큼 저절로 채워집니다.

> ⚠️ **이것은 관측을 남기는 범위지 분석의 `--day-type`이 아닙니다.** 평일과 휴일을
> 섞어 통계를 내지 말라는 규약은 그대로입니다(COLLECTOR.md 3장).

> 🔴 **한쪽 PC에서는 `--status`가 영원히 "결측"이라고 말합니다.** 창이 07:00–23:00
> (하루 97틱)인데 회사환경은 낮만, 집환경은 저녁만 켜져 있기 때문입니다 —
> 2026-09-03 기준 회사환경은 하루 49~61틱이고 **온전한 날이 0일**입니다.
> **고장이 아닙니다.** 온전함은 `merge_stock.py`로 **합친 뒤에야** 판정할 수
> 있습니다. 한쪽 숫자만 보고 창을 다시 좁히지 마십시오.
>
> ✅ **이 경고는 2026-09-05(1.26.121)부터 `--status` 출력에도 들어 있습니다.**
> 여기에만 적어 둔 동안 실제로 오독이 났습니다 — 1.26.120이 이 문서를 안 보고
> "9월 들어 수집이 고장났다"고 판단했습니다. 이제 도구가 `결측`은 판정이
> 아니라는 것과 `merge_stock.py`를 함께 출력하고, **가동 구간**을 보여 줘
> 결측이 *꺼져 있던 시간*임을 표 안에서 알 수 있습니다.

### 왜 도로는 한 환경만인가

패널이 git으로 공유되고 `startTime`도 같아서 **두 환경이 사실상 같은 값을
받습니다.** 재고와 달리 **표본이 늘지 않는데** TMAP 일일 호출 한도만 두 배로
씁니다 — 키를 공유한다면 그만큼 잘립니다. (요금 문제가 아니다 — TMAP은 무료
요금제이고 한도는 횟수다, 1.26.210 정정.)

집환경이 맡는 이유는 **가동 시간**입니다. 도로 수집은 재고와 달리 **하루치가
통째로 걸려 있어**(놓치면 그날이 빕니다) 늦게까지 켜져 있는 쪽이 유리합니다.

> 📌 **2026-09-03에 회사환경의 도로 수집을 껐습니다.** 그날 집환경에 등록(1.26.103)하면서
> 두 곳이 동시에 도는 상태가 됐기 때문입니다. 데이터는 지우지 않았고 작업은
> `pause`(Disabled)로 남아 있습니다.
>
> 🔴 **다만 남아 있는 것은 1.26.105 이전 등록(평일 03:30 한 번, `--if-needed` 없음)이라
> `resume`으로 되살리면 안 됩니다** — 주말·공휴일 계수가 쌓이지 않습니다(2026-09-14 조회).
> 이쪽으로 도로 수집을 옮길 때는 관리자 권한 PowerShell에서
> `.\scripts
oad_collector.ps1 install`로 **다시 등록**하십시오(COLLECTOR_ROAD.md 11-1장).

### 이미 모은 것은 어디에 있나

**도로 관측 2일치(09-01·09-02)는 (2026-09-03 기준) 회사환경 DB에만 있었습니다.**
집환경으로 옮기려면 파이프라인 실행이 아니라 **수집기 자료**이므로
`transfer_run.py`가 아니라 [DB_이관.md 3-B장](DB_이관.md#3-b-수집기-자료-옮기기--재고-시계열tmap-실측)의
`export_collected.py`·`merge_stock.py`를 씁니다. **2026-09-09에 옮겼습니다**
(아래 4-3장의 병합 기록 참고 — `roadprobe-2026-09-02` 400구간).

**재고는 양쪽에 따로 쌓이므로 `tools/merge_stock.py`로 합쳐야 합니다.**
2026-09-03 기준 회사환경 DB에는 **평일 08-25~09-03치만** 있고 휴일이 한 건도
없습니다 — 집환경분을 아직 합치지 않았기 때문입니다.

### 바꿀 때 할 일

1. 수집기를 켜거나 끕니다(`road_collector.ps1` · `collector.ps1`).
2. **위 표를 고칩니다.**
3. 도로 수집을 옮겼다면 **양쪽의 패널 지문이 같은지** 확인합니다 —
   `python tools/collect_road_time.py --status`의 `지문` 줄.

---

## 0-A. 회사환경 주말 가동 — **회사에서 이 순서대로** (2026-09-13)

> 0장의 결정을 실행하는 절차서입니다. **회사 PC 앞에서 위에서 아래로** 따라
> 하십시오. 각 단계에 *무엇을 기대하는지*를 함께 적었습니다 — 다르면 멈추고
> 그 값을 들고 오십시오.
>
> 🔴 **설정이 맞아 보여도 실측 전에는 모릅니다.** 집환경이 그렇게 물렸습니다
> (09-08에 "주말에 켜 둔다"고 정했는데 첫 주말 자동 수집이 45틱/1틱이었습니다).

### 1단계 — 전원 설정 두 곳

**설정 → 시스템 → 전원 및 배터리 → 화면 및 절전**

| 항목 | 값 |
| --- | --- |
| 전원 연결됨 · 다음 시간 후 장치를 **절전 모드로 전환** | **안 함** |
| 전원 연결됨 · 다음 시간 이후에 내 화면 끄기 | 그대로 두어도 된다 |

**제어판 → 전원 옵션 → 덮개를 닫으면 수행되는 작업**

| 항목 | 값 |
| --- | --- |
| **전원 사용** 시 덮개를 닫을 때 | **아무 것도 안 함** |
| 배터리 사용 시 | 바꿀 필요 없다 |

📌 **화면 끄기는 건드리지 않아도 됩니다.** 집환경 실측에서 *전원 연결 + 덮개
닫음* 상태로 **8시간 6분간** S0 진입이 한 번도 없었고 수집이 정상이었습니다.
화면이 꺼지는 것 자체는 문제가 아닙니다 — **전원이 빠지는 것**이 문제입니다.

> 📌 **회사환경 실측이 위 두 설정의 효과를 한 번 보여 줬습니다 (2026-09-14).** 이 PC는
> 집환경과 달리 **화면이 꺼지면 대기(S0)에 들어갑니다**(점심 12:51). 그런데 두 설정을 바꾼 뒤의
> 51분 대기는 **절전 단계로 넘어가지 않아 틱 5/5**였고, 바꾸기 전의 충전 중 대기 5구간은
> 절전 단계로 넘어가 **틱 0**이었습니다. 🔴 그래서 **'절전 → 안 함'이 핵심**입니다 — 화면
> 끄기는 그대로 두어도 되지만, 이 한 줄을 빠뜨리면 충전기를 꽂아도 멈춥니다. 한 번의
> 관측이라 5단계에서 틱이 비면 [COLLECTOR.md 7장](COLLECTOR.md)의 `SleepEntered` 확인
> 명령을 함께 보십시오.

### 2단계 — 확인 (세 줄)

```powershell
powercfg /a                                              # 어떤 절전 방식인가
powercfg /query SCHEME_CURRENT SUB_SLEEP STANDBYIDLE     # 절전까지 (AC, DC)
powercfg /query SCHEME_CURRENT SUB_SLEEP HIBERNATEIDLE   # 최대절전까지 (AC, DC)
```

| 보는 곳 | 기대값 |
| --- | --- |
| `/a` | *"대기 모드 (S0 저 전원 유휴)"* 가 있으면 **Modern Standby 기기** — 전원 연결이 필수다 |
| `STANDBYIDLE` | 끝의 두 16진수가 AC·DC다. **AC가 `0x00000000`이면 "안 함"** ✅ |
| `HIBERNATEIDLE` | 절전과 **별개 타이머**다. AC가 0이 아니면 최대 절전으로 내려갈 수 있다 |

⚠️ **`HIBERNATEIDLE`을 빠뜨리지 마십시오.** 절전만 껐는데 최대 절전이 켜져
있으면 몇 시간 뒤 똑같이 멈춥니다.

### 3단계 — 재부팅 대비 (🔴 절전보다 이쪽이 더 위험할 수 있다)

`PBR재고수집`은 **`InteractiveToken`** 으로 등록돼 있어 **로그인 세션이 있어야만**
돕니다. 잠금 화면은 괜찮지만 **재부팅은 다릅니다** — 주말에 Windows 업데이트가
자동 재부팅하면 아무도 로그인하지 않아 **월요일까지 통째로 멈춥니다.**

둘 중 하나를 고르십시오.

| | 방법 | 장단 |
| --- | --- | --- |
| **A** | Windows 업데이트 **활성 시간**을 넓히고 주말 재부팅을 미룬다 | 간단·되돌리기 쉬움 · 업데이트가 강제되면 뚫린다 |
| **B** | 작업을 **로그오프 상태에서도** 돌게 다시 등록한다 | 재부팅에 강함 · **비밀번호를 작업에 저장**한다 |

```powershell
# B를 고른 경우 — 로그인 비밀번호를 한 번 묻는다
.\scripts\collector.ps1 install -Window 00:00-23:50 -IncludeHolidays -RunWhenLoggedOff
```

🔴 **B의 대가를 먼저 보십시오.** ① 회사 정책상 작업에 비밀번호를 저장하는 것이
금지일 수 있습니다. ② **비밀번호를 바꾸면 작업이 조용히 멈춥니다** — 주기적
변경이 강제되는 환경이면 그때마다 다시 등록해야 합니다.

📌 **판단이 서지 않으면 A로 두고 첫 주말 결과를 보십시오.** 재부팅이 실제로
일어나지 않으면 B는 필요 없습니다.

> 🔴 **밤중 재시작은 이미 있었습니다** (2026-09-15 이벤트 로그로 확인). **09-10 00:45**에 Windows 업데이트
> 구성요소(`MoUsoCoreWorker` · `TrustedInstaller`, `#1074`)가 PC를 다시 시작했습니다 — 이 PC의 활성 시간은
> **08~18시**라 그 밖은 막지 못합니다. 그날은 하루 종일 틱이 0인 원인 미상의 날이기도 합니다(단서일 뿐).
> ⚠️ 활성 시간은 **최대 18시간**이라 A만으로는 6시간이 늘 열려 있습니다 — 주말·추석처럼 긴 무인 기간에는
> **업데이트 일시 중지**(설정 → Windows 업데이트)를 함께 거는 편이 확실합니다. **결정: 09-18(금) 퇴근 전.**

### 4단계 — 자리를 뜨기 전에

```powershell
.\scripts\collector.ps1 status
```

- **`스케줄 : 활성`** 과 **다음 실행** 시각이 보이는지
- **`마지막 : ... 성공`** 인지 (`요청 거부됨`이면 1~2단계를 다시 본다)
- 전원 어댑터가 **실제로 꽂혀 있는지** — 이것 하나가 전부를 좌우합니다

### 5단계 — 월요일(09-21)에 판정

```powershell
.\scripts\collector.ps1 status
```

| 결과 | 뜻 |
| --- | --- |
| 토·일이 각 **80틱 이상** | ✅ 성공 — 주말 분담이 자리 잡았다 |
| 수십 틱에서 끊김 | ⚠️ 중간에 잠들었다 — `HIBERNATEIDLE`·어댑터, 그리고 대기가 **절전 단계로 넘어갔는지**(`SleepEntered`, [COLLECTOR.md 7장](COLLECTOR.md))를 본다 |
| 거의 0틱 | 🔴 재부팅이나 로그오프 — 3단계 B를 고려한다 |

> 창이 07:00–23:00(97틱)이라 **한쪽 PC만으로는 영원히 "결측"** 입니다. 그것은
> 고장이 아닙니다(0장 경고) — **판정은 합친 뒤에** 합니다.

### 6단계 — 이왕 회사 PC 앞이라면 **함께 들고 오십시오**

두 가지가 집환경에서 막혀 있습니다. 한 번에 해결됩니다.

```powershell
# ① 정본 스냅샷 — 게이트 A 채택이 이것 없이는 ④단계에서 멈춘다
python tools\transfer_run.py --list                     # 라벨이 있는지 먼저
python tools\transfer_run.py --export "2026-08-11 real" --out data\transfer\run_20260811_real.db

# ② 재고 수집분 — 지금 집환경의 "온전 0일"은 합치기 전의 반쪽짜리 값이다
#    data\raw_data\재고이력\ 폴더를 통째로 복사해 온다
```

집환경에서 받는 쪽 명령은 [4-2](#4-2-산출물--옮기지-않아도-된다-약-135gb)·
[4-3](#4-3-재고이력은-성격이-다르다--합쳐야-한다)에 있습니다.

📌 **②를 합쳐야 진짜 공백이 보입니다.** 이동 중 구멍이 실제로 얼마인지,
보조배터리 같은 추가 대책이 필요한지는 그때 판단합니다.

---

## 1. 네 갈래 — 무엇을 어떻게 다루나

| 갈래 | 대상 | 조치 | 왜 |
| --- | --- | --- | --- |
| **재생성** | `.venv/` · `__pycache__/` · `.pytest_cache/` · `.vscode/` | **옮기지 않는다** | 옮기면 오히려 깨진다 (2장) |
| **손으로 1회** | `.env` | **키 값만 따로 나른다** | git에 넣으면 안 되는 비밀 (3장) |
| **손으로 1회** | `data/raw_data/` | **한 번 복사** | 재생성 불가·1.5GB (4장) |
| **재생성** | `data/bike_system.db` · `data/pp_data/` | **옮기지 않는다** | 파이프라인 산출물 (4장) |
| **라벨 이관** | 정본 스냅샷 실행 1건 | `tools/transfer_run.py` — **절차서: [DB_이관.md](DB_이관.md)** | 라이브 재고라 **재생성 불가** (4-2장) |

`docs/연구/references/`(106MB, 논문 PDF)는 5장에서 따로 다룹니다.

---

## 2. 옮기면 안 되는 것 — 가상환경과 캐시

`.venv/`를 복사하면 **깨집니다.** 안의 스크립트에 파이썬 실행 파일 경로가
절대경로로 박히기 때문입니다. 사용자 이름이 다른 PC로 옮기면 그 경로가 없습니다.

각 PC에서 새로 만드는 것이 정답이고, 그 방법은 이미 [README.md 설치](../../README.md#설치)에
있습니다. `requirements.txt`가 커밋되어 있으므로 **이것이 사실상의 동기화 장치**입니다.

```powershell
py -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
python -m pip install -r requirements-dev.txt   # 테스트까지 돌리려면
```

> ⚠️ **버전을 맞추세요.** 두 PC의 파이썬이 다르면 한쪽에서만 나는 버그를 쫓게
> 됩니다. 검증된 조합은 [README.md 검증된 환경](../../README.md#검증된-환경-2026-08-10-전체-파이프라인-e2e-확인)에
> 표로 있습니다(Python 3.14.7 기준). `requirements.txt`는 하한(`>=`)만 걸므로
> **설치 시점이 다르면 버전이 갈릴 수 있습니다** — 한쪽에서 이상하면 먼저
> `pip freeze`로 양쪽을 대조하세요.

`__pycache__/`는 두면 오히려 해롭습니다 — 파이썬 버전이 다르면 무시되고, 같으면
낡은 바이트코드가 새 소스를 가릴 여지가 있습니다. 지우면 다음 실행에서 다시 만듭니다.

---

## 3. `.env` — 키 이름은 git이, 키 값은 손이 나른다

**`.env`는 절대 커밋하지 않습니다.** API 키가 들어 있고, 한 번 올라가면 이력에
남아 지워도 남습니다.

대신 [.env.example](../../.env.example)이 **키 이름과 설명을 나릅니다.** 이 파일은
커밋되므로 새 PC에서는 이것을 복사해 값만 채우면 됩니다.

```powershell
copy .env.example .env
```

### 무엇을 채워야 하나

| 키 | 필수 | 쓰는 곳 |
| --- | --- | --- |
| `TASHU_API_KEY` | **필수** | step0 재고 수집 · 웹 실시간 대조 |
| `API_KEY` (TMAP) | **필수** | step3 경로 지도 |
| `APIHUN_KMA_TYPE01_KEY` | 선택 | 실시간 날씨 — **없어도 파이프라인은 돕니다** |

기상청 키가 선택인 이유는 [.env.example](../../.env.example) 주석에 적힌 그대로입니다 —
과거 날씨 자료는 키 없이 CSV로 받고([WEATHER.md](../분석/WEATHER.md) 1장), 키는
실시간 관측·예보에만 씁니다.

### 기상청 활용신청은 옮길 것이 없다 (2026-08-31 실측)

기상청 API 허브는 **키 하나로 엔드포인트 여러 개**를 쓰는데, 각각 **따로
활용신청**해야 열립니다. 그런데 **승인은 키 문자열이 아니라 계정에 붙습니다** —
같은 키를 쓰는 두 PC는 한쪽에서 신청이 승인되면 **양쪽이 동시에 열립니다.**
**동기화할 것이 없다는 뜻입니다.** 키를 새로 발급받지 않았다면 `.env`도 그대로 둡니다.

2026-08-31 기준 실제 응답으로 확인한 상태입니다.

| 엔드포인트 | 쓰는 곳 | 상태 |
| --- | --- | --- |
| `kma_sfctm2` | 실시간 관측 (`fetch_current`) | ✅ 열림 |
| `fct_afs_dl` | 문장 예보 (`fetch_forecast`) | ✅ 열림 |
| `nph-dfs_shrt_grd` | 격자 예보 · 강수량 mm (`fetch_grid`) | ✅ 열림 |
| `kma_sfctm3` | **기간 관측** (여러 시각 한 번에) | ❌ **403 — 미신청** |

`kma_sfctm3`만 아직 막혀 있습니다. **막혀 있어도 지장은 없습니다** —
[weather.py](../../weather.py)가 이 경우를 예상하고 시각별 조회로 우회하며,
긴 기간은 기상자료개방포털 CSV로 받습니다. 다만 **한쪽 PC에서 403이 났다고 해서
그 PC만의 문제로 보지 마세요** — 계정 단위라 양쪽이 똑같이 막혀 있습니다.

**키 값 자체는 git이 아닌 경로로 나릅니다.** 세 개뿐이므로 비밀번호 관리자나
본인에게 보내는 메시지면 충분합니다. 클라우드 드라이브에 `.env`를 두고 양쪽에서
링크하는 방법도 있지만, **그 드라이브가 동기화 중 충돌을 내면 키가 반쪽이 됩니다** —
파일이 작으므로 손으로 두 번 만드는 편이 안전합니다.

> 💡 **키를 새로 발급받았다면 양쪽을 함께 고치세요.** 한쪽만 고치면 다른 PC에서
> 인증 실패가 나는데, 원인이 코드가 아니라 `.env`라는 것을 알아채기까지 시간이
> 걸립니다.

---

## 4. `data/` — 원본만 옮기고 나머지는 다시 만든다

`data/`는 약 2.8GB로 **git으로는 옮길 수 없습니다.** GitHub 파일 상한이 100MB인데
DB 하나가 1.3GB입니다. 그런데 **전부 옮길 필요도 없습니다** — 안이 두 종류로 갈립니다.

### 4-1. 원본 — 반드시 옮긴다 (약 1.5GB)

받아 온 자료라 **다시 만들 수 없습니다.**

| 경로 | 크기 | 무엇 |
| --- | --- | --- |
| `data/raw_data/타슈 대여이력(25.04~26.03)/` | **1.5GB** | 1년치 대여이력 원본 |
| `data/raw_data/날씨/` | 550KB | 기상자료개방포털 ASOS CSV (2025·2026) |
| `data/raw_data/재고이력/` | 8.2MB | 수집기가 쌓은 일별 재고 — 4-3장 |

USB나 클라우드 드라이브로 한 번 복사하면 됩니다. **대여이력이 사실상 전부**이므로
날씨·재고이력은 부담이 없습니다.

### 4-2. 산출물 — 옮기지 않아도 된다 (약 1.35GB)

| 경로 | 크기 | 만드는 것 |
| --- | --- | --- |
| `data/bike_system.db` | 1.3GB | [db.py](../../db.py) — "파이프라인 산출물을 한 파일에 모아 관리한다" |
| `data/pp_data/` | 56MB | step0~step4 (ILP·VRP·성능 지표·순수요 등) |

원본만 있으면 `python run_pipeline.py`로 다시 만들어집니다.

> 🔴 **예외가 하나 있습니다 — 실행 라벨은 다시 만들어지지 않습니다.**
> `station_info.stock`은 [tashu.py](../../tashu.py)가 **실행하는 순간 라이브
> API에서 받은 재고**입니다(`parking_count` → `stock`). 2026-08-11에 돌린 실행의
> 재고는 그날로 끝났고, 오늘 다시 돌리면 **오늘 재고**가 들어옵니다.
>
> 그런데 [DECISIONS.md](../분석/DECISIONS.md) 6-B가 실험의 **정본 스냅샷**을
> 특정 실행 라벨(`2026-08-11 real`)로 못박았습니다. 그 라벨이 없는 PC는
> [TODO.md](../기록/TODO.md)의 `P4-실행대기` 명령을 **하나도 돌릴 수 없습니다.**
> 실제로 2026-08-31에 그 상태가 됐습니다 — 한쪽 PC에는 그 라벨이 있고 다른
> 쪽에는 없었는데, 양쪽 문서가 각자의 DB를 보고 서로 반대되는 사실을 적었습니다.
>
> **DB 1.3GB를 통째로 옮길 필요는 없습니다.** 라벨 하나만 떼어 나르는 도구가
> 있습니다(약 6천 행, 수백 KB).
>
> ```powershell
> # 라벨을 가진 PC에서
> python tools/transfer_run.py --list
> python tools/transfer_run.py --export "2026-08-11 real" --out run_20260811.db
>
> # 받는 PC에서
> python tools/transfer_run.py --import run_20260811.db --dry-run
> python tools/transfer_run.py --import run_20260811.db
> ```
>
> **이미 있는 라벨은 말없이 덮지 않습니다**(`--overwrite`가 있어야 덮습니다) —
> 실험 결과가 붙어 있는 실행을 덮으면 어느 쪽 숫자를 본 것인지 알 수 없게
> 되기 때문입니다. `road_leg`(TMAP 실측)는 **따라가지 않습니다.** 양쪽이 각자
> 쌓는 관측치라, 섞으면 '어느 PC에서 잰 것인가'가 사라집니다 — 재고이력과
> 같은 성격이라 4-3장처럼 각자 쌓는 쪽이 맞습니다.

> #### 왜 git으로 나르지 않는가 — **재고 확인 (2026-09-01)**
>
> *"`.gitignore`에서 `*.db`를 빼면 되지 않나"* 를 실측으로 따져 보고 **현행 유지로
> 결정했습니다**(사용자 결정).
>
> **먼저 `*.db`를 빼도 아무것도 달라지지 않습니다.** 1.3GB DB를 막는 것은
> `.gitignore`의 **`data/`**(19행)이지 `*.db`(33행)가 아닙니다
> (`git check-ignore -v data/bike_system.db`로 확인). `data/` 밖에는 `.db`가
> 하나도 없으므로, 실제로 커밋하려면 **`data/`를 풀어야** 하고 그때 아래가
> 전부 현실이 됩니다.
>
> | 잃는 것 | 실측 |
> | --- | --- |
> | 저장소 폭발 | 압축률 31% → **커밋 1회당 약 404MB.** 현재 `.git`은 **6.6MB**다 |
> | GitHub 상한 | 100MB 초과라 **푸시가 거부된다.** LFS를 써도 무료 1GB를 두세 번에 소진 |
> | 되돌릴 수 없음 | 한 번 커밋하면 이력에 영원히 남는다 — 빼려면 `filter-repo` + 강제 푸시 |
> | 델타가 안 먹힘 | SQLite는 **3바이트만 바뀌어도 블롭을 통째로** 새로 저장한다(실측) |
> | 병합 불가 | 바이너리라 양쪽이 파이프라인을 돌리면 **매번 한쪽을 버려야** 한다 |
> | 규약 충돌 | `merge_stock.py`는 *"먼저 수집한 것이 이긴다"*로 관측을 지키는데 git 병합은 **한쪽을 덮는다** |
>
> 📌 **1.3GB 중 실제로 못 나르는 것은 1.4MB뿐**이고, 그것을 위한 도구가
> `transfer_run.py`입니다. 나머지는 파이프라인이 다시 만듭니다.
>
> #### 그래서 정한 방식 — **파일로 내보내 손으로 옮긴다**
>
> 내보낸 파일은 **`data/transfer/`**에 둡니다(`data/`가 이미 무시되므로 git에
> 딸려 가지 않습니다). USB·클라우드 드라이브 등으로 옮기십시오.
>
> ```powershell
> # 내보내는 PC
> python tools/transfer_run.py --export "2026-08-11 real" --out data/transfer/run_20260811_real.db
>
> # 받는 PC (같은 경로에 둔 뒤)
> python tools/transfer_run.py --import data/transfer/run_20260811_real.db --dry-run
> python tools/transfer_run.py --import data/transfer/run_20260811_real.db
> ```
>
> **왕복이 손실 없음을 확인했습니다**(2026-09-01) — 빈 DB에 import해 대여소
> 1,368곳·총재고 3,235로 원본과 일치했습니다. 12개 테이블 9,124행, 파일 1.4MB.
>
> ⚠️ **`road_leg`(TMAP 고정 패널)는 따라가지 않습니다** — 양쪽이 각자 쌓는
> 관측치라 섞으면 '어느 PC에서 잰 것인가'가 사라집니다(4-3장의 재고이력과 같은
> 성격). 2026-09-01부터 양쪽에서 각자 쌓입니다.

> ⚠️ **DB 전체는 다만 시간이 듭니다.** 1년치 대여이력을 다시 훑어야 하므로, **재생성 시간이
> 복사 시간보다 길면 DB째 복사하는 편이 빠릅니다.** 1.3GB 파일 하나이므로 판단
> 기준은 단순합니다 — 새 PC를 자주 세팅하지 않는다면 **한 번은 DB를 통째로 옮기고**
> 이후 갱신만 파이프라인에 맡기는 것이 현실적입니다.

### 4-3. 재고이력은 성격이 다르다 — 합쳐야 한다

`data/raw_data/재고이력/`은 **양쪽 PC가 각자 쌓습니다.** 복사가 아니라 **병합**이
필요한 유일한 자료입니다. 덮어쓰면 한쪽 관측이 사라집니다.

병합기가 따로 있습니다 — `INSERT OR IGNORE`라 **이미 있는 틱은 건드리지 않습니다.**

```powershell
python tools\merge_stock.py D:\다른PC\재고이력\ --dry-run   # 무엇이 들어올지 먼저
python tools\merge_stock.py D:\다른PC\재고이력\
```

자세한 규칙(겹칠 때 무엇이 이기는지, 마스터가 왜 CSV에 없는지)은
[COLLECTOR.md 11-4장](COLLECTOR.md#11-4-합치기-toolsmerge_stockpy)이 정본입니다.

> ✅ **`--dry-run`은 재고·마스터·TMAP 실측을 모두 셉니다.** 도로는
> `roadprobe-*` 라벨별 구간 수까지 보여 줍니다. *"재고만 보여 준다"* 는 결함을
> 1.26.162에서 찾아 `count_new()`로 고쳤고, 예행은 실제 저장과 **같은 열쇠**로
> 셉니다. 다른 패널로 잰 회차가 섞였는지는 출력의 라벨 목록과
> `python tools/collect_road_time.py --status`로 확인하십시오.

**실제 병합 기록 (2026-09-09, 회사환경 → 집환경).** `collected_회사환경_
2026-09-09.db`(88만 행) 기준입니다.

| 갈래 | 결과 |
| --- | --- |
| 재고 | **+376,140행** · 487,174행은 이미 있어 무시(먼저 수집한 값이 이긴다) |
| 도로 | **+400구간** = `roadprobe-2026-09-02` 하루치 (집 PC에 없던 날) |
| 누적 | `stock_history` **106.8만 행** · `quick_check=ok` |

🔴 **다른 패널로 잰 회차가 섞이는 것을 두 겹으로 막습니다.** 보낸 쪽이
`roadprobe-2026-09-01`이 옛 패널이라고 알렸는데, 확인해 보니 **집 DB의 09-01도
이미 같은 회사 자료**였습니다(양쪽 다 현재 패널과 70/100 일치). `road_leg`의
기본키가 `(run_label, duration, cluster, leg)`이라 300행 전부 키가 겹쳐
`INSERT OR IGNORE`가 무시했고, 설령 들어왔더라도 `road_time_model.py`가
실행 첫 줄에서 *"지금 패널과 다른 구간이 93개 있습니다 → 제외하고 잽니다"*
로 걸러냅니다. **판정용 10일은 09-02부터 셉니다.**

---

## 5. `docs/연구/references/` — git 밖에 두되 연구는 막히지 않는다

논문 PDF 원본 106MB입니다. [.gitignore](../../.gitignore)에 **"저작권·용량 때문에
커밋하지 않는다"**고 근거가 적혀 있으므로 이 방침은 유지합니다.

**연구 진행에는 지장이 없습니다** — 읽고 정리한 내용은
[LITERATURE.md](../연구/LITERATURE.md)와 [references/README.md](../연구/references/README.md)에
남고 이 둘은 커밋됩니다. **원문이 필요한 것은 인용 쪽수를 확인할 때 정도입니다.**

그런 일이 잦다면 클라우드 드라이브에 두고 양쪽에서 참조하세요. `data/`와 달리
용량이 작아 동기화 대상으로 무리가 없습니다.

---

## 6. 정리 — 새 PC 세팅 순서

```powershell
# 1. 코드 — git이 나른다
git clone https://github.com/movinggeol/PBR-PublicBicycleRelocation-.git
cd PBR-PublicBicycleRelocation-

# 2. 가상환경 — 각 PC에서 새로 만든다 (2장)
py -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt -r requirements-dev.txt

# 3. 키 — 이름은 git이, 값은 손이 (3장)
copy .env.example .env
#    → TASHU_API_KEY · API_KEY 채우기

# 4. 원본 데이터 — 한 번 복사 (4-1장)
#    data/raw_data/ 를 USB·클라우드로

# 5. 산출물 — 다시 만든다 (4-2장)
python run_pipeline.py

# 6. 확인
python -m pytest
```

**이후 번갈아 작업하는 것은 git만으로 됩니다.** 위 세팅은 PC당 한 번입니다.

---

## 7. 자동 동기화를 넣지 않은 이유

두 PC가 **같은 DB 파일을 동시에 쓰면 SQLite 잠금이 걸리고, 어느 쪽이 정본인지
흐려집니다.** 클라우드 드라이브에 `data/`를 통째로 올려 양쪽에서 여는 구성이
특히 위험합니다 — 1.3GB 파일을 백그라운드에서 동기화하는 도중에 쓰기가 들어가면
**파일이 깨질 수 있습니다.**

같은 판단이 재고 병합에도 적용되어 있습니다 — 자동이 아니라 **분석 직전에 손으로
한 번 부르는 명령**입니다([COLLECTOR.md 11-5장](COLLECTOR.md#11-5-남는-일)).

---

## 관련 문서

| 문서 | 내용 |
| --- | --- |
| [COLLECTOR.md](COLLECTOR.md) | 11-1(겹쳐도 안전한 이유)·11-2(요일 옵션)·11-4(`merge_stock.py` 병합). 11장 머리의 평일/휴일 분담은 2026-09-03 폐기 — **분담의 정본은 이 문서 0장** |
| [README.md](../../README.md) | 설치·검증된 환경·환경변수 |
| [.env.example](../../.env.example) | 키 이름과 설명 (커밋됨 — 이것이 키 목록의 정본) |
| [WEATHER.md](../분석/WEATHER.md) | 기상청 키가 왜 선택인지, 과거 자료 받는 법 |
| [DB_SCHEMA.md](DB_SCHEMA.md) | 병합 대상 `stock_history` 스키마 |
