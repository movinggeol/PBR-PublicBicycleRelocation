# TODO — 해야 할 것 / 고쳐야 할 것

> 2026-08-07 최초 작성, **2026-08-20(1.17.3) 재점검**. 전체 코드(step0~step4,
> run_pipeline.py, project_config.py, db.py, webapp/, tests/)와 문서(README.md, docs/)를
> 전수 검토한 결과입니다.
> 우선순위: 🔴 파이프라인이 깨지는 문제 / 🟡 결과가 틀리거나 문서와 다른 문제 / 🟢 품질·유지보수 개선
>
> **이 문서는 시스템의 과제만 다룹니다.** 졸업작품·논문에 필요한 것(비교 대조군,
> 반복 실행 통계, 선행연구·문제 정형화)은 [THESIS.md](../연구/THESIS.md)에 따로 있습니다.
>
> **2026-08-07 업데이트(버전 1.0.3)**: 🔴 P1 전체와 🟡 P2 대부분이 수정 완료되었습니다.
> 완료 항목은 아래 "✅ 완료" 섹션으로 이동했습니다.

---

## 🔴 P1. 대조군 실험이 새로 드러낸 것 (1.18.0)

논문용 대조군 비교([EXPERIMENTS.md](../분석/EXPERIMENTS.md) 5장)를 만들면서 **평가 자체의
결함 세 가지**가 드러났습니다. 셋 다 지금까지의 성과 수치를 낙관적으로 만들고 있습니다.

### 1-1. 🔴 마지막 depot 복귀가 거리·시간에서 빠져 있다

`vrp.greedy_route()`는 작업이 모두 끝나면 **그 자리에서 멈춥니다.** `return` 행을 만드는
코드는 "적재가 막혀 후보가 없을 때" 분기 하나뿐인데, **그 분기는 ILP를 거친 입력에서
실행될 수 없습니다.** ILP가 군집마다 총 pick = 총 drop으로 맞춰 주므로
`남은 drop = 남은 pick + 적재량`이 성립해, 후보가 비는 상태가 생기지 않습니다
(무작위 균형 입력 300회 시행에서 복귀 0건, 잔량 0대).

즉 **가끔 빠지는 것이 아니라 항상, 구조적으로 빠집니다.** 실데이터 15개 실행·회차
1,224행에 `return` 행이 **0건**인 이유입니다. 최종 복귀를 기록하는 코드는 애초에
없습니다.

- 설계는 "차량 1대 = 클러스터 1개 + **depot 복귀**"입니다([FLEET.md](../구현/FLEET.md)).
- 그래서 총 이동거리·최장 소요시간이 복귀분만큼 **과소 추정**이고,
  **시간 예산 120분 판정도 그만큼 낙관적**입니다.
- **실측했습니다** (25년 11월 평일, 클러스터 30개):

  | 회차 | 누락된 복귀 거리 | 예산 초과 (현재) | 복귀를 포함하면 |
  | --- | --- | --- | --- |
  | `_05_10` | 84.2 km | 2건 | **5건** |
  | `_10_15` | 98.9 km | 0건 | **3건** |
  | `_15_20` | 103.2 km | 1건 | **5건** |
  | **합계** | **286.3 km** (총 이동의 33%) | **3 / 30** | **13 / 30 (43%)** |
- 고칠 때: `greedy_route()`의 루프 종료 직전에 복귀 구간을 추가하면 됩니다.
  **단, 고치면 예산 초과가 3건 → 13건으로 드러납니다.**
- ⏸ **지금은 고치지 않기로 했습니다 (사용자 결정, 2026-08-21).** '차량 1대 =
  클러스터 1개' 구조에서는 클러스터를 더 쪼갤 수 없어, 초과를 없애려면
  **작업량을 줄이거나 회차당 차량을 늘리는 운영 결정**이 필요합니다. 둘 다 현장
  확인이 있어야 하는 사안입니다. 그때까지는 **측정된 한계로 문서에 남깁니다**
  ([THESIS.md](../연구/THESIS.md) 권장 순서 7번). 고칠 때는 README·EXPERIMENTS의 소요시간
  수치를 전부 다시 재야 합니다.

### 1-2. ~~🔴 step4의 결품 지표로는 방법 간 비교가 불가능하다~~ → **완료 (1.18.4)**

step4는 **계획량(`rebal_qty`)이 전부 집행됐다고 보고** 재고를 더했습니다. 그 기준으로 재면
제안 방법·그리디·지리 균등 군집·**무재배치까지 네 방법의 점수가 전부 0.42h로 같습니다.**
계획은 같고 집행만 다른데 계획을 재고 있었기 때문입니다.

**조치**: `stockout_simulation()`이 **VRP가 실제로 싣고 내린 양**으로 재고를 더합니다
(`executed_delta()`). 계획 기준 값은 `stockout_hours_plan`으로 함께 남겨
**계획과 집행의 격차**를 볼 수 있게 했습니다. VRP 결과가 없는 구버전 산출물에서는
경고를 찍고 계획량으로 물러섭니다.

- `experiments/baseline/baseline_compare.py`도 이제 **같은 함수**를 씁니다 — 측정 코드와 운영
  코드가 갈리지 않습니다.
- 재발 방지 테스트 4개 추가(`tests/test_calculations.py`): 부호·합산·복귀 제외,
  집행 기준과 계획 기준이 실제로 다른지, 폴백.
- ⚠️ **1.18.4 이전에 쌓인 `stockout_hours_after`는 계획 기준입니다.** 실행 간 비교 시 주의.

### 1-3. ~~🟡 계획은 평일로 세우고 평가는 평일·휴일을 섞어서 한다~~ → **완료 (1.18.3)**

`calculate_target_qty`는 `select_day_type()`으로 한쪽만 골라 통계를 내는데,
step4의 `load_net_demand()`는 **필터 없이 그 기간 전체를 읽고 있었습니다.**
평일 계획을 주말 수요로도 채점하고 있었다는 뜻입니다.

**조치**: `load_net_demand()`가 `config.day_type`으로 거릅니다. `day_type`은 실행 시점에
이미 해석된 값이라(`auto` → weekday/holiday), **휴일 계획은 휴일 순수요로, 평일 계획은
평일 순수요로** 자동으로 평가됩니다. 몇 일치를 썼는지 콘솔에 찍습니다.
재발 방지 테스트 2개 추가(`tests/test_day_type.py`, 평일·휴일 각각).

> 대조군 실험(`experiments/baseline/baseline_compare.py`)은 처음부터 요일 구분을 적용했으므로
> [EXPERIMENTS.md](../분석/EXPERIMENTS.md) 5·6장과 README의 수치는 **영향받지 않습니다.**
> 바뀌는 것은 파이프라인이 `kpi_summary`에 쌓는 값입니다.

### 1-4. 🟡 초기 재고 스냅샷은 요일 구분과 무관하다

1-3을 고쳐 **순수요**는 요일 구분을 따르게 됐지만, **초기 재고(`stock`)** 는 여전히
`tashu_api.py`가 **실행 시점에 받아 온 현재 스냅샷**입니다.

- 평일 낮에 돌려 휴일 계획을 세우면, **평일의 재고 분포로 휴일 결품을 시뮬레이션**합니다.
- 순수요(과거 달)와 재고(현재)의 **시점이 다르다**는 기존 한계와 같은 뿌리입니다
  ([RETROSPECTIVE.md](RETROSPECTIVE.md) 6장).
- 고치려면 요일 구분별 재고 스냅샷을 쌓아야 합니다(`station_stock`에 `day_type` 추가 +
  수집 시각 기록). **1.20.0부터 `stock_history`가 10분 간격으로 쌓이고 있습니다.**
- **1.21.0에서 첫 실측을 냈습니다** (`experiments/structure/observed_stockout.py`).
  복원 대비 실측 결품이 `_10_15` 기준 **+13%**. 다만 **수집 하루치(37틱)** 라 잠정값이고,
  온전한 날이 며칠 쌓여야 인용할 수 있습니다. 수집 창이 09~17시라 `_05_10`·`_15_20`은
  한 시각만 겹쳐 아직 비교가 안 됩니다.
- 덤으로 드러난 것: **관측의 48.8%가 재고 0**이고 1,372곳 중 261곳은 내내 0이었습니다.
  → **1.21.9에서 확인: pick 후보는 멀쩡했습니다.** 후보의 재고 0 비율 0%,
  못 채우는 양 3.5%. 48.8%는 전체 평균이고 pick 후보는 '재고가 남아도는 곳'만
  고른 집합이라 겹치지 않습니다. 다만 `_15_20`만 90%로 약하고, 수집 창이
  17시까지라 그 회차의 절반(18~20시)은 아직 모릅니다.
- 논문에는 이렇게 쓰십시오: *"초기 재고는 수집 시점의 현재 스냅샷이므로, 계획 대상일의
  요일 구분과 반드시 일치하지는 않는다."*

---

## 🟡 P2. 남은 동작 문제

### 2-1. `vrp.py` — 남은 모델링 결정

- ~~`VEHICLE_TOTAL = 21` 미사용~~ → 해소. 회차당 투입 상한(`VEHICLES_PER_ROUND`)이
  클러스터 수를 제한하고, 실제 차량이 배정된다 ([FLEET.md](../구현/FLEET.md)).
- ~~ILP 25km/h vs VRP 30km/h 불일치~~ → **해소(1.13.2). 25km/h로 통일**하고
  `project_config.VEHICLE_SPEED_KMPH` 한 곳에서 읽는다(사용자 결정).
- ~~차량이 여러 클러스터를 순차 처리하지 못함~~ → **채택하지 않기로 결정(1.13.2).**
  한 회차에 차량 1대 = 클러스터 1개 + depot 복귀 구조를 유지한다(사용자 결정).
  확장하지 않는 이상 회차 소요시간은 클러스터 하나의 크기로 결정되므로,
  시간 예산 관리는 **클러스터를 어떻게 묶느냐(step1)**의 문제로 남는다.
- VRP 작업시간은 "자전거 1대당 30초" 가정 — 현장 값으로 검증 필요.
- 🟡 **시간 예산 초과가 다시 생겼다 (25km/h 전환의 결과)** — **1.21.6에서 스위치를
  달았다**(`--enforce-time-budget`, 기본 꺼짐). 켜면 초과 4건 → 0건, 최장 196분 →
  117분이고 대가는 결품 +0.011h(40초)다. 원인도 쟀다 — 소요시간을 지배하는 것은
  대여소 수(r=0.80)가 아니라 **이동 거리(r=0.966)** 이고, 초과 군집은 45~56km로
  정상(30km)보다 흩어져 있다. **남은 것**: ① 현장 확인 후 기본값을 켤지 결정,
  ② `wanted_vehicles()`의 이동 추정에 거리 항 추가, ③ 군집 목적함수의 거리 가중치
  `α` 재실험(γ가 비단조였으므로 3회차 전부로).
  초과를 표시·경고하지만(1.8.1) 끈 상태에서는 계획을 바꾸지 않는다. 속도를 25로 낮추자 같은 경로가 3건 초과로 바뀌었다
  (`_05_10` 2건, `_15_20` 1건, 최장 115.8 → 132.0분). **계획이 나빠진 게 아니라
  추정이 정확해진 것**이다 — 경로·거리는 한 글자도 안 바뀌었다.
  실측상 소요시간의 70~80%가 이동이므로 `γ` 상향이 가장 직접적인 대응책이지만,
  γ는 비단조라 재실험이 필요하다(15번과 함께 다룰 것). 대안은 회차당 투입 대수를
  늘려 클러스터를 잘게 쪼개는 것.
- 🟡 **greedy 경로의 최적성 갭이 평균 9.7%, 최악 46.4%다** (1.18.0 실측,
  [EXPERIMENTS.md](../분석/EXPERIMENTS.md) 6장). 전면 교체는 하지 않기로 했지만,
  **갭이 20%를 넘는 클러스터(30개 중 5개)만 OR-Tools로 다시 푸는 절충안**은
  싸게 손해의 대부분을 회수합니다. 재현: `experiments/baseline/ortools_gap.py`.
- ~~🟡 **`TOP_STATION_LIMIT = 50`이 계획 규모를 정하는데 근거가 없다**~~ →
  **측정 완료, 50이 맞다 (1.21.8)**. 결품은 상한이 클수록 단조롭게 좋아지지만
  (50→100에서 3.4~5.2% 감소) **필요 차량이 보유 21대를 넘어 잘린다**(상한 70에서
  24.7대, 100에서 31.5대). 그러면 차량당 대여소가 5.1 → 8.3곳으로 늘어 1.21.6이
  밝힌 예산 초과 조건이 된다. **50은 보유 차량과 맞물린 값**이었다.
  **남은 것**: 차량을 늘릴 수 있다면 상한 100 + 31대로 결품 5% 개선이 가능하다 —
  파라미터가 아니라 **운영 결정**이다. 재현: `experiments/params/top_limit_sweep.py`
  > 이 항목은 1.21.7에서 **작업 문턱을 재다가** 나왔다. `REBAL_MIN_QTY = 2`를
  > 바꿔도 결과가 안 변해서 파고드니, 자격자(pick 76~89곳·drop 208~298곳)를
  > **상한 50이 먼저 자르고 있었다.** 문턱이 아니라 상한이 규모를 정한다.
- `VEHICLES_PER_ROUND = 10`의 현장 근거 확인 필요. (1.13.0부터 보유 대수·회차당 대수
  모두 웹 실행 폼과 `--fleet-size`/`--vehicles-per-round`로 실행마다 조정할 수 있으므로,
  현장 값이 정해지면 기본값만 바꾸면 된다.)

---

## 🟡 P2-B. 의존성 폐기 예고 — **대비 완료 (1.18.7), 전환은 남음**

### `PULP_CBC_CMD`가 PuLP 4.0에서 사라진다

`pulp 3.3.2`가 실행마다 경고를 냅니다. `requirements.txt`는 **하한(`>=`) 고정**이라
PuLP 4.0이 나온 뒤 새 환경에서 설치하면 **step2가 통째로 깨집니다.** 1.2.1에서
"의존성이 전부 import 실패"를 겪은 것과 같은 종류의 사고입니다.

**한 것 (1.18.7)**

| 항목 | 조치 |
| --- | --- |
| `LpVariable.dicts` (폐기 예정) | → `prob.add_variable_dicts()`. 경고 사라짐 |
| 솔버 생성 | `ilp.build_solver()` 하나로 모음. 파이프라인·실험·테스트가 전부 이걸 쓴다 |
| 폐기 대비 | `PULP_CBC_CMD` → `COIN_CMD`(PATH) → `COIN_CMD`(cbcbox 경로) → **무엇을 설치할지 알려 주고 중단** |
| 회귀 방지 | 테스트 3개 (폐기 후 폴백, 없을 때 안내, main이 직접 만들지 않는지) |

**순서에 뜻이 있습니다** — `PULP_CBC_CMD`를 **먼저** 봅니다. 문서의 모든 수치가
그 솔버로 나왔으므로, 있는 동안에는 그대로 써서 재현성을 지킵니다.

**남은 것 — 확인해 본 결과**

- `pip install pulp[cbc]`는 **`cbcbox` 패키지(약 149MB)** 를 설치합니다. 설치하면
  `COIN_CMD(path=cbcbox.cbc_bin_path())`로 정상 동작합니다(실측 확인).
- 다만 **PuLP 3.3.2의 `COIN_CMD`는 cbcbox를 자동으로 찾지 못합니다** — 경로를 직접
  넘겨야 합니다. `build_solver()`가 그 처리를 이미 해 둡니다.
- **149MB를 모두에게 강요하지 않으려고 `requirements.txt`에는 넣지 않았습니다.**
  현재 PuLP가 CBC를 동봉하므로 아직 필요 없습니다.
- **PuLP 4.0이 실제로 나오면**: (a) 4.0이 여전히 CBC를 동봉하는지 확인 →
  아니라면 (b) `requirements.txt`를 `pulp[cbc]>=…`로 바꾸고 README의 "별도 solver
  설치가 필요 없습니다" 문구를 정정. 상한(`pulp<4`)은 규약 위반이라 쓰지 않습니다.

---

## 🟡 P2-C. 수정안 19~26번의 남은 항목 (1.22.0에서 셋, 1.22.1에서 둘을 처리했다)

2026-08-26 추가분입니다. **7·12·14·16번은 이미 되어 있었고**(표 정렬·폴더 구조·
목차·용어집), 21번 전반(대조표 정렬)·23번(실행 이름)·9번(날씨 문서)은
[1.22.0](버전관리.md)에서 고쳤습니다. 남은 것은 아래입니다.

### C-1. ~~🟡 성과 지표 툴팁을 클릭으로 고정하기~~ → **완료 (1.22.1)**

`#tipbox`에 고정 상태(`data-pinned`)와 닫기 단추를 더했습니다. 전 화면이 같은
장치를 쓰므로 KPI 밖에서도 함께 동작합니다.

⚠️ **고정은 `.tip` 딱지에서만 합니다.** 내비 링크와 화면 전환 단추에도 `data-tip`이
붙어 있어, 아무 `data-tip`에서나 클릭을 가로채면 누르는 순간 고정만 되고
**페이지 이동이 막힙니다.** 처음에 그렇게 짰다가 발견해 좁혔습니다.

### C-2. ~~🟡 차량 운용 지표가 어느 기간인지 화면에 없다~~ → **완료 (1.22.1)**

*"상단 3가지 지표 + 아래의 차량별 누적은 하루 동안의 작업내용인가?"* —
**아닙니다. 하루치가 아니라 DB에 쌓인 모든 실행·회차의 합계입니다.**
`db.vehicle_workload()`의 SQL에 기간 조건이 아예 없습니다(`vehicle_assignment`
전체를 `SUM`). 형평성은 누적으로만 보이므로 이 자체는 의도된 설계입니다.

**함께 드러난 것**: 타일 셋의 기간이 서로 달랐습니다. 출동·작업량 차이는
`vehicle_workload()`(전체)에서, 시간 예산 준수는 `vehicle_assignments()`(선택한
실행으로 좁혀짐)에서 옵니다. 실행을 고르면 **한 줄에 서로 다른 기간의 숫자가
나란히** 놓였습니다. 계산은 그대로 두고 각 숫자가 어느 기간인지 화면에 밝혔습니다.

### C-3. 🟢 순수요 기간을 1년치로 확장 (수정안 19번) — **조사 먼저**

지금은 특정 한 달(`25년 11월`)로 고정해 통계를 냅니다. raw_data에는 1년치가
있는데 한 달만 씁니다. 사용자가 제시한 두 방향:

- **하드스플릿** — 달·계절로 끊어 해당 구간만 쓴다.
- **소프트스플릿** — 날씨·날짜를 종합해 가중한다(관련 없는 것은 비중을 거의 0으로).

⚠️ 먼저 확인할 것: **계절 보정(warmup)이 이미 비슷한 일을 하고 있습니다** —
대상 달 첫 14일로 도시 전체 배율을 구해 곱합니다. 소프트스플릿이 이것과 겹치는지,
대체하는지부터 정해야 합니다. 날씨 원천은 이미 13,848시간이 준비돼 있습니다
([WEATHER.md](../분석/WEATHER.md)). 판정은 반드시
`experiments/structure/quantile_model_eval.py`의 기준(작업 대상만·평일/휴일 따로·
표본 밖·베이스라인 초과)을 통과해야 합니다 — 1.15.2의 실패가 그 때문이었습니다.

### C-4. ~~🟢 시각화 대안 조사~~ → **완료 (조사만, 1.22.2)** — [VISUALIZATION.md](../분석/VISUALIZATION.md)

*"바로 수정하지 말고 조사 정도만"* 이라 하셔서 **코드는 한 줄도 바꾸지 않았습니다.**

**결론: 형식(html)이 문제가 아니라 지금 html이 자립하지 못하는 것이 문제입니다.**
지도 1장이 **CDN 5곳의 자원 11개**를 받아야 열리고, Leaflet 본체까지 외부에 있어
**인터넷이 끊기면 빈 화면**입니다. 과거 산출물의 재현도 CDN에 달려 있습니다.

이것은 **저장소가 이미 내린 결정과 어긋납니다** — `webapp/charts.py`의 그래프는
정확히 반대(외부 JS 없음, 인라인 SVG, *"인터넷이 끊겨도 화면이 같아야 한다"*)입니다.
지도만 예외입니다.

**권장**: 라이브러리를 갈아엎지 말고 **지금 folium을 자립시킬 것**. 자원 한 벌
(666 KB, 실측)을 `webapp/static/`에 두고 참조하면 파일 크기는 그대로입니다.
⚠️ `catalog.ALLOWED_SUFFIXES`가 `.html`/`.csv`뿐이라 `data/` 아래에 `.js`를 두면
서빙되지 않습니다.

**하지 말 것**: PNG 전환(25번 호버 팝업과 정면충돌), plotly 전환(자립시키면
한 장에 +4.35 MB — 실측). `charts.py`의 인라인 SVG 방식은 그대로 둡니다.

### C-5. 🟢 지도 호버 팝업·UI/UX (수정안 25·26번)

25번은 마우스를 대면 정보가 뜨게, 26번은 html을 유지한다면 UI/UX 개선.
24번 조사 결과가 나온 뒤에 손대는 것이 순서입니다.

---

## 🟢 P3. 품질·유지보수 개선

1. ~~**폴더명 정리**~~ → **완료(1.18.3).** 여섯 폴더를 ASCII로 바꿨다:
   `step0_collect` / `step0_eda` / `step1_cluster` / `step2_optimize` /
   `step3_map` / `step4_metrics`. 셸에서 따옴표로 감쌀 필요가 없어졌고,
   `step0 (raw…)`와 `step0(전처리…)`가 공백 유무만 달라 헷갈리던 문제도 사라졌다.
   - ~~**남은 것**: `1.top_st_clustering.py`가 숫자로 시작해 일반 import가 안 된다~~
     → **완료(1.18.8).** `top_st_clustering.py`로 바꿨다(15개 파일의 참조 동시 수정).
     `버전관리.txt`의 과거 기록은 그때의 이름을 그대로 뒀다.
2. ~~**스키마 검증**~~ → **완료(1.18.8).** `project_config.require_columns()`를
   step1·step2(ILP·VRP)·step4의 읽는 자리에 붙였다. 컬럼이 빠지면 **어느 산출물의
   무엇이 없는지 밝히며 즉시 멈춘다** — 뒤 단계에서 KeyError로 죽거나 조용히 틀린
   값을 내는 것보다 낫다.
3. **성능**: `adjust_module.try_move_node()`가 노드 이동 시도마다 `pick_drop.copy()` +
   전체 목적함수 재계산(내부에서 `compute_medoids` 반복) — 버전관리.txt에 기록된
   "5분 이상 소요" 문제의 원인. 이동 노드가 속한 두 군집만 재계산하는 증분 방식으로 개선.
4. **VRP 고도화**: 현행은 greedy 휴리스틱(단일 차량/클러스터). OR-Tools 등
   전용 VRP solver로 다차량·시간창·실도로 거리 반영.
5. **데이터 관리**: CSV → SQLite 이관 — **1~4단계 완료**
   (저장소 + 이중 기록 + 웹 API 전환 + 대여이력 적재).
   남은 단계는 [DB_PLAN.md](../구현/DB_PLAN.md) 참고
   (5. 실행 이력 비교 화면 → CSV 기록·폴백 제거). 스키마 상세는 [DB_SCHEMA.md](../구현/DB_SCHEMA.md).
   - **외래키 미선언** — 이중 기록 중이라 일부러 안 걸었다(DB 기록 실패가 연쇄 실패가
     되면 안 됨). CSV를 정본에서 내리는 5단계에서 `runs` 부모 + `ON DELETE CASCADE`로
     선언 검토. 지금 `PRAGMA foreign_keys=ON`은 선언된 FK가 없어 무동작이다.
   - **`idx_metrics_run` 중복** — `metrics`의 PK `(run_label, duration, station_id)`가
     만드는 자동 인덱스의 왼쪽 접두사와 같다(`EXPLAIN QUERY PLAN`으로 확인).
     지워도 조회 계획이 같고 쓰기 비용만 줄지만, 이득이 미미해 보류.
   - ~~**마이그레이션 장치 없음**~~ → 해소(1.13.1). `init_schema()`가 `migrate_schema()`로
     빠진 컬럼을 `ALTER TABLE ADD COLUMN` 한다. 1.10.0 이전 DB에서 `kpi_summary`의
     `stockout_hours_*`가 없어 지표가 조용히 안 쌓이던 문제가 사라졌다.
     **남은 것**: 컬럼 이름 변경·삭제·타입 변경은 여전히 수동이다(데이터 손실 위험이라
     의도적으로 자동화하지 않음). 기본값 없는 NOT NULL 추가도 SQLite 제약으로 불가 —
     경고만 낸다. 그런 변경이 실제로 필요해지면 그때 테이블 재생성 절차를 만들 것.
6. **EDA 시각화**: `month_graph`에 matplotlib 그래프 통합
   (`experiments/learning/matplotlib_month_graph.py`의 한글 폰트 설정 참고).
7. ~~**step1 매직 넘버**~~ → **완료(1.18.8).** 여섯 개를 `project_config`로 뺐다:
   `REBAL_MIN_QTY`(2) · `TOP_STATION_LIMIT`(50) · `TARGET_CLUSTER_SIZE`(7) ·
   `ADJUST_MAX_ITER`(200) · `ADJUST_BALANCE_OK`(3) · `ADJUST_BALANCE_LIMIT`(5).
   - ⚠️ **여섯 개 다 실험으로 정한 값이 아니라 관행값**이다. `z`·`γ`와 달리 재실험
     기록이 없으므로, 논문에서 근거를 묻는다면 그렇게 답해야 한다
     ([FORMULATION.md](../분석/FORMULATION.md) 3·4장에 명시).
8. **버전관리.txt → CHANGELOG.md 승격 검토.**
9. ~~**README 주요 결과 수치의 산출 근거**(입력 데이터·실행 시점) 기록.~~ → 완료(1.18.0).
   기간·요일 구분·씨앗·실행일을 표 아래에 명시하고, 재현 명령을 experiments/README에 뒀다.
10. **webapp**: SSE 로그 스트리밍(현재 3초 meta-refresh),
    외부 공개 시 인증 (docs/구현/WEBAPP.md 참고).
11. **테스트 확장** ([TESTING.md](../구현/TESTING.md)): ~~CI(GitHub Actions) 연결~~ → 완료(1.18.0,
    `.github/workflows/tests.yml`). **남은 것**: 단계별 계산 로직(목표재고 공식, 군집 조정,
    VRP 적재 제약)의 단위 테스트. 지금은 여전히 스모크 수준이다.
12. **KPI 체계 구축**: ①~⑤단계 전부 완료(1.11.0). 포화 시간·수요 충족률은 미구현.
13. ~~**백테스트가 드러낸 문제 3가지**~~ → 셋 다 실험 완료
    ([EXPERIMENTS.md](../분석/EXPERIMENTS.md), 1.12.0). 남은 것은 아래 14~16번.

14. ~~🔴 **계절 전환기 배율 보정(warmup) 구현**~~ → **완료(1.15.1).**
    `--warmup-days`(기본 14)로 계획 대상 달 첫 N일 실적에서 도시 전체 배율을 구해
    `mu`·`sigma`에 곱한다. 계절 전환 달(26년 2→3월) `_05_10` 커버리지 88.7% → 94.7%.
    자료가 없으면 조용히 건너뛴다. 아래는 당시 근거 기록이다.

    ~~실험 근거는 이미 충분하다~~
    ([EXPERIMENTS.md](../분석/EXPERIMENTS.md) 3장). 분석 달 첫 14일 실적으로 도시 전체
    배율 하나를 구해 `mu`·`sigma`에 곱하면, 계절 전환 달의 `_05_10` 커버리지가
    86.5% → 95.6%로 회복된다(MAE도 1.520 → 1.491로 함께 개선).
    - `z`를 올리는 것으로는 대체되지 않는다 — z=2.10에서도 전환 달은 90.4%.
    - 구현 부담: `calculate_target_qty.py`가 지금은 학습 달 하나만 받는다.
      **분석 달의 부분 실적**이라는 새 입력이 필요하다(`--warmup-period` 등).
    - 배율은 반드시 **도시 전체 하나**로만 추정할 것. 며칠치로 대여소별 보정을
      하면 잡음만 커진다. 안전장치로 [0.5, 2.0] 범위 제한을 둔다.

15. 🟡 **`γ = 3000`을 다른 달에서 재확인** — 현재 근거는 25년 11월 한 달뿐이다
    ([EXPERIMENTS.md](../분석/EXPERIMENTS.md) 4장). γ에 대한 결과가 **비단조**라
    (γ=2000이 γ=1000보다 나빴다) 보간으로 추정할 수 없고, 계절이 다른 달에서
    직접 돌려봐야 한다. 26년 03월(봄) 자료가 이미 적재돼 있다.

16. 🟡 **중심 통계를 평균에서 중앙값으로 바꿀지 검토** —
    대여소 중앙값이 전 회차·전 무리에서 평균보다 근소하게 우세하다
    (전체 MAE 2.6~5.0% 개선, [EXPERIMENTS.md](../분석/EXPERIMENTS.md) 2장).
    수요가 0 근처인 대여소에서 자연스럽게 0으로 수렴해, 현행 `mu`가 그런 곳에서
    0 예측보다 나쁜 문제(-2.8~-7.5%)를 완화한다.
    - **단독으로 바꾸면 안 된다.** `target_qty = mu + z·sigma`에서 중심만 중앙값으로
      바꾸면 산포(표준편차)와 통계적으로 어긋난다. MAD 등 로버스트 산포와 함께
      다뤄야 하고, `z`도 다시 잡아야 한다.
    - 순수요가 정수라 중앙값이 0으로 몰려 작업 대상 선정이 크게 흔들릴 수 있다.
    - 이득(MAE 3~5%)에 비해 파급이 크므로 우선순위는 낮다.

17. 🟡 **날씨를 수요 예측에 붙일지 — 측정은 끝났고, 붙이는 것만 남았다 (1.20.1)**

    자료를 받았고(대전 133, 2025-01 ~ 2026-07, 13,848시간) 읽는 코드(`weather.py`)와
    측정(`experiments/structure/weather_impact.py`)까지 끝냈다. 결과는
    [WEATHER.md](../분석/WEATHER.md)에 있다. **채택 기준을 통과했다.**

    - 작업 대상 대여소·표본 밖·계절 보정을 켠 베이스라인 대비 MAE **+4.3%**
      (검증 달 51건 중 45건 개선). 그런데 이 평균은 성격을 감춘다 —
      **비 오는 날(전체의 10%)에 +40%, 나머지 날엔 +0.4%다.**
    - 붙일 자리는 `demand_model.add_weather()` 하나다(학습·예측이 같은 함수를
      거쳐야 한다 — 날씨는 날짜별이라 대여소별인 `build_features()`에 못 들어간다).
      방식은 계절 배율과 같은 **도시 전체 하루 배율**.
    - **머신러닝(분위수 모델)로도 시도했고 여전히 채택 기준 미달이다.** 날씨가 모델을
      낫게는 하지만(0.6%p → 0.2%p) 과잉이 늘어 맞바꿈이다. 비 오는 날엔 확실히
      낫고(과잉 -16~-33%) 맑은 날엔 조금 나쁘다 — 90%인 맑은 날이 이득을 상쇄한다.
      피처와 하네스는 갖춰 뒀으니 표본이 쌓이면 재판정은 명령 한 줄이다.
    - **반영 방식은 정해졌다 (사용자 결정, 2026-08-25): 알림만 한다.** 계획을 세우는
      화면이 지금 날씨를 띄우고 계획량은 건드리지 않는다(1.20.2 구현 완료).
    - **남은 것은 예보다.** 측정은 관측값으로 했고, 운영은 예보를 써야 하며 예보
      오차만큼 이득이 줄어든다. 사용자가 **동네예보 API 활용신청을 하기로 했다** —
      열리면 `weather.py`에 예보 클라이언트를 붙이고, 화면의 '지금 관측' 자리를
      '오늘 예보'로 바꾸고, ④(대여소 단위 판정)를 **예보 기준으로 다시 잰다.**
      그 결과를 보고 자동 반영(계획량에 배율 적용)을 다시 논의한다.
    - 기간 조회 API(`kma_sfctm3`)는 **아직 403**이다. 활용신청을 해야 열린다.
      지금 키로는 `kma_sfctm2`(한 시각)만 된다.

18. ~~🟢 **이상치 제거가 계획 경로에 걸리지 않는다**~~ → **측정 완료, 지금이 옳다 (1.20.8)**

    `concat_1year_file.py`의 IQR × 1.5 필터가 계획 경로에 안 걸리는 것이 결함인지
    쟀다. 재현: `python experiments/structure/outlier_impact.py`

    - 옮기면 **작업 대상의 13.4%가 뒤바뀐다** — 무해하지 않다.
    - 그런데 **IQR이 자르는 것이 오류가 아니었다.** 이용시간 실측 범위가 0~46분인데
      (60분 초과 0건 — 원천이 이미 캡돼 있다) IQR 상한이 33.5분이라 **정상 상위 4.4%**를
      자른다. 33분 자전거 이용은 이상한 일이 아니다.
    - 순수요가 **실제로 쓰는 네 필드**(대여·반납 시각, 대여소 ID)는 결함 0건이다.
      결측 0, 시각 역전 0, 마스터에 없는 대여소 0.
    - **결론: 필터를 옮기지 않는다.** 자세한 것은 [DECISIONS.md](../분석/DECISIONS.md) 6-1.

19. ~~🟢 **노션의 초기 정리 문서를 docs에 반영**~~ → **완료 (1.20.9)**

    수정안 10번. [ORIGINS.md](ORIGINS.md)로 옮겼다. 노션 페이지가 클라이언트 렌더링이라
    본문이 안 잡혀 공개 페이지 API(`loadPageChunk`)로 하위 페이지까지 954개 블록을 받았다.

    **문서에 없던 것이 여럿 나왔다** — 관제센터 통화로 뒤집힌 적재량 가정(15대 → 10대),
    거리뷰로 거치대 수를 수작업 검증한 대여소 4곳, 클러스터링 세 번의 시행착오,
    `z = 1.65`(단측 95%)를 고른 원래 근거.

---

## ✅ 완료 (2026-08-20, 버전 1.17.3) — 코드·웹·문서 정합성 점검

전체 코드와 웹 화면, 문서를 맞대어 본 라운드입니다. **셋 다 테스트가 없던 자리에서
나왔습니다.**

| 문제 | 어떻게 드러났나 | 조치 |
| --- | --- | --- |
| `run_pipeline.py`가 `--warmup-period` / `--warmup-days`를 **거절**했다 | 문서에 적힌 명령을 그대로 실행 | 인자 선언 + 하위 단계 전달. `--warmup-days 0`은 값 판정에서 사라지므로 `is not None`으로 거른다 |
| `/kpi`가 결측 지표에서 **500** | `save_kpi`에 일부 지표만 넘겨 재현 | 템플릿의 정수 표기를 결측 안전하게(`whole` 매크로) |
| 폼·안내의 시간대 예시가 `10_15` | 예시대로 입력하면 step4 `duration_hours()`가 IndexError | 실제 형식(`_10_15`)으로 정정, 기간 예시도 `25년 11월`로 |
| base.html 내비 풍선에 "차량 21대" 하드코딩 | 대수를 바꿔도 안 따라옴 | 숫자를 뺐다(내비에는 설정값을 넘길 수 없다) |
| `PBR_VEHICLE_SPEED_KMPH`가 `.env.example`에 없음 | 코드의 `PBR_*` 목록과 대조 | 추가 |
| `PBR_WEATHER_FILE`을 **아무도 읽지 않는데** 동작하는 것처럼 설명 | 사용처 검색 | 미사용임을 코드·`.env.example`에 명시하고 위 17번으로 남김 |

문서 최신화: 테스트 수(184 → 200), `PROJECT_PIPELINE.md`의 목표 재고 공식(`z` 1.65 →
1.99 + 계절 보정·요일 구분), `RETROSPECTIVE.md` 통계와 ⑦장, `README`의 실행 옵션 표와
`/guide` 라우트, `WEBAPP.md` 템플릿 목록, `step0_eda.md`의 없어진 경로(`test/test.py`).

재발 방지 테스트 4개를 함께 넣었습니다 — 옵션 전달, 결측 지표 렌더링,
입력 예시 형식 2개.

~~**고치지 않고 남긴 것**~~ → **완료(1.18.8).** `st_visualization.py`의
`cluster_center_file`과 주석 처리된 마커 코드를 지웠습니다(어느 단계도 만들지 않는
파일을 읽고 있었습니다). 함께 **글롭도 좁혔습니다** —
`store.CSV_FALLBACK["pick_drop"]`이 `top*.csv`에서 `top_[0-9]*.csv`로 바뀌어
`top_center*.csv` 같은 다른 산출물을 잡지 않습니다.

---

## ✅ 완료 (2026-08-12, 버전 1.12.0) — 파라미터 실험 3건 ([EXPERIMENTS.md](../분석/EXPERIMENTS.md))

백테스트가 드러낸 세 문제를 각각 실험했습니다. 재현 스크립트는 `experiments/`에 있습니다.

| 실험 | 물음 | 결과 | 조치 |
| --- | --- | --- | --- |
| `z_sweep.py` | `z=1.65`가 95%를 덮나 | 92.5%. z=1.99에서 94.9% | `TARGET_Z = 1.99` |
| `predictor_compare.py` | `_10_15`의 `mu`는 쓸모없나 | **측정이 틀렸다.** 작업 대상만 보면 **+40.2%** | 회차 유지, 측정 기준 정정 |
| `seasonal_window.py` | 계절 전환기를 어떻게 넘나 | 분석 달 첫 14일로 배율 보정 시 86.5% → 95.6% | 권고 (14번) |
| (전체 실행) | z 변경의 실제 효과 | 결품 -10~27%, `_15_20` 예산 초과 | `CLUSTER_GAMMA = 3000` |

**측정 대상이 틀리면 정반대 결론이 나옵니다.** `_10_15`의 `mu`가 "0 예측과 차이 없다"던
것은, 전체 1,369곳 평균이라 파이프라인이 손대지도 않는 곳(72.8%가 `|mu| < 0.5`)이
결과를 지배한 탓이었습니다. 이 회차를 없앨 뻔했습니다.

**지표가 파라미터에 의존하면 그 파라미터 실험에 쓸 수 없습니다.** 개선률·목표
도달률은 `target_qty`를 분모로 삼아, z를 올리면 결품이 줄어도 함께 떨어집니다.
z가 다른 실행끼리는 결품 시간으로 비교해야 합니다 → [KPI.md](../분석/KPI.md)에 규칙 추가.

**`z`와 `γ`는 연동됩니다.** z를 1.99로 올리자 처리 대수가 681 → 756으로 늘어
`_15_20`이 137분으로 예산을 넘었고, γ를 3000으로 올려 흡수했습니다
(전 회차 최장 137.0 → 115.8분, 초과 0건, 총거리 870 → 854km).

---

## ✅ 완료 (2026-08-12, 버전 1.11.0) — 수요 예측 백테스트 (KPI 5단계)

파이프라인의 모든 지표가 `target_qty`를 기준으로 계산되는데, 그 근거인 `mu`가
맞는지는 **한 번도 검증된 적이 없었습니다.** `tools/backtest_demand.py`로
한 달의 `mu`가 다음 달을 얼마나 맞히는지 쟀습니다(연속 월 9쌍, 대여소 1,369곳).

| 시간대 | MAE | 기준선(0 예측) | 개선 | 커버리지 | 95%에 필요한 z |
| --- | --- | --- | --- | --- | --- |
| `_05_10` | 1.29 | 1.66 | **+22.5%** | 91.7% | 2.07 |
| `_10_15` | 1.14 | 1.14 | **−0.2%** ⚠ | 92.7% | 1.98 |
| `_15_20` | 1.57 | 1.77 | +11.0% | 92.7% | 1.96 |
| `_20_05` | 1.44 | 1.53 | +6.0% | 92.8% | 1.94 |

세 가지가 드러났습니다:

1. **출근 시간대에서만 예측이 확실히 쓸모 있습니다.** `_10_15`는 "순수요 0"이라고
   찍는 것과 차이가 없습니다 — 낮에는 대여·반납이 균형을 이뤄 순수요가 0 근처를
   무작위로 오가기 때문으로 보입니다.
2. **`z = 1.65`가 부족합니다.** 정규분포 가정값인데 실제 분포의 꼬리가 두꺼워
   목표 95%에 못 미칩니다. 실측 필요값은 **z ≈ 1.99**.
3. **편향은 없습니다**(±0.01대). 평균은 잘 맞고, **분산을 과소평가**하는 것이 문제입니다.

계절 전환기(2월→3월)에는 커버리지가 86.5%까지 떨어집니다 — 대여량이 1.8배 뛰는
구간을 한 달 전 평균이 따라가지 못합니다.

## ✅ 완료 (2026-08-12, 버전 1.10.0) — 결품 시간 시뮬레이션 (KPI 4단계)

지금까지의 지표는 전부 **"계획이 목표 재고를 얼마나 채웠나"**(계획 달성률)였습니다.
순수요로 재고 궤적을 복원해 **이용자가 자전거를 못 타는 시간**을 재는 첫 지표입니다.

```text
stock(t+1) = clip(stock(t) − 순수요(t), 0, 거치대 수)
결품 시간 = stock(t) ≤ 0 인 시간대 수
```

| 회차 | 결품 시간(전) | 결품 시간(후) | 감소 | 개선 / 악화 |
| --- | --- | --- | --- | --- |
| `_05_10` | 2.30h | **0.50h** | −1.80h | 50곳 / **0곳** |
| `_10_15` | 2.00h | 0.28h | −1.71h | 50곳 / 0곳 |
| `_15_20` | 1.79h | 0.34h | −1.45h | 50곳 / 0곳 |

**Pick 대여소에서 결품이 늘어난 경우가 한 건도 없었습니다.** 회수의 부작용이
없다는 뜻이라 검증해 보니 근거가 분명했습니다 — Pick 대여소는 회수 후에도 재고
중앙값 16대인데 5시간 순수요가 **−0.7대**(반납이 더 많음)라 바닥날 일이 없고,
Drop 대여소는 **재고 중앙값이 0대**라 결품이 거의 전부 여기서 났습니다.

한계도 분명합니다: 시뮬레이션이지 실측이 아니고, 초기 재고(API 현재 스냅샷)와
순수요(25년 11월)의 시점이 다르며, 재고를 0에서 자르므로 결품 시간의 **하한**입니다.

테스트 6개 추가 (전체 117개) — 시뮬레이션 계산을 직접 검증합니다(재고 소진,
거치대 상한, 음수 방지, 자정 넘는 시간대).

## ✅ 완료 (2026-08-12, 버전 1.9.3) — KPI 요약 테이블과 `/kpi` 화면

지표가 콘솔·CSV·여러 테이블에 흩어져 있어 실행 간 비교를 매번 손으로 맞춰야 했습니다.
**실행 1건(run_label + duration) = 1행**으로 모아 자동으로 쌓이게 했습니다.

| 항목 | 내용 |
| --- | --- |
| `kpi_summary` 테이블 | 규모·계획·운영·효율·품질 16개 지표. 미구현 지표(결품·수요오차)는 NULL로 자리만 |
| step4 자동 기록 | `save_kpi_summary()`가 metrics·route_summary·vehicle_assignment를 모아 저장 |
| `/kpi` 화면 | 헤드라인 4개 + **직전 실행 대비 증감** + 실행·회차별 표 |
| `/api/kpi` | `run_label`·`duration` 필터 |
| 테스트 13개 추가 (전체 111개) | 멱등 저장, 부분 지표 NULL, 실행 간 비교 쿼리, 화면·API |

**첫 실측이 바로 지표의 값을 증명했습니다:**

| 회차 | 평균 개선률 | 목표 도달 | km당 개선 |
| --- | --- | --- | --- |
| `_05_10` | 65% | **6%** | **2.08대** |
| `_10_15` | 71% | 46% | 1.56대 |
| `_15_20` | 71% | 37% | 1.55대 |

`_05_10`은 개선률이 가장 낮아 보이지만(65%) **km당 개선은 가장 높습니다**(2.08대).
반대로 목표 도달은 6%뿐입니다 — drop 필요량이 pick 가능량보다 훨씬 커서
각 대여소가 조금씩만 채워지기 때문입니다. **개선률 하나만 봤다면 정반대로 판단**했을
상황이고, KPI.md 7장의 "평균은 분포를 감춘다"가 첫 실측에서 그대로 나타났습니다.

## ✅ 완료 (2026-08-12, 버전 1.9.2) — 거리 가중치 실험, 시간 예산 초과 해소

step1 목적함수의 **거리 항이 balance에 묻혀 사실상 무시되고 있던 문제**를 찾아 고쳤습니다.

```text
초기:  balance 18,475 × 1 = 18,475   |  거리 0.81 × 10 = 8.1    (2,280배)
최종:  balance    179 × 1 =    179   |  거리 1.97 × 10 = 19.7   (9배)
```

조정 과정에서 balance는 좋아졌지만 **거리는 2.4배 나빠졌고**(군집이 흩어짐),
그래서 44km짜리 클러스터가 생겨 시간 예산을 넘겼습니다.

| γ | 최장 소요 | 예산 초과 | 총 이동거리(3회차) | balance 최대 | 처리 대수 |
| --- | --- | --- | --- | --- | --- |
| 10 (기존) | 121.0분 | 1건 | 865.7 km | 5 | 684 |
| **1000 (현재)** | **110.6분** | **0건** | **814.4 km** | 6 | 681 |

**최장 -8.6%, 초과 1건→0건, 총 이동거리 -51km(-6%)**. 대가는 balance 5→6으로 미미합니다.
가중치는 `project_config`의 `CLUSTER_ALPHA/BETA/GAMMA`로 분리했습니다
(TODO의 "step1 매직 넘버 설정화" 일부 해소).

실험에서 배운 것:

- **γ 10~150은 차이가 없습니다** — 거리 항이 완전히 묻혀 이동 자체가 안 일어납니다
- **결과가 비단조입니다** — γ=2000에서는 오히려 133.9분. greedy 탐색이라 국소 최적이
  흔들리므로 **단일 실행으로 판단하면 안 됩니다**. 처음에 한 회차만 보고
  "거리가 늘어난다"고 잘못 읽었다가 3회차 합계에서 뒤집혔습니다
- **γ=5000은 과합니다** — 총 거리는 최소지만 balance가 19로 폭발하고 처리 대수 -6%
- 25년 11월 한 달 기준이므로 계절이 다른 달에서 재확인할 가치가 있습니다

## ✅ 완료 (2026-08-12, 버전 1.9.1) — 실데이터 하루 3회차 검증

사용자의 실제 운용 모델(하루 3회차, 회차당 10대, 로테이션)을 실데이터로 확인했습니다.

| 회차 | 차량 | 후보 | 처리 | 이동 | 최장 | 개선률 |
| --- | --- | --- | --- | --- | --- | --- |
| `_05_10` | V01~V10 | 310곳 | 305대 | 301 km | 121.0분 ⚠ | 65% |
| `_10_15` | V11~V20 | 152곳 | 182대 | 265 km | 86.5분 | 71% |
| `_15_20` | V21 외 9대 | 167곳 | 197대 | 300 km | 119.6분 | 71% |

- **로테이션 실증**: 순번표 없이 누적 기록만으로 의도한 교대가 나왔습니다.
  **21대 전원 출동, 미출동 0대.**
- **형평성**: 출동 1~2회(편차 1회), 누적 76~151분(편차 74.9분). 편차가 큰 이유는
  회차당 10대 × 3회차 = 30슬롯인데 차량이 21대라 9대가 두 번 나가기 때문입니다.
  구조적이라 하루로는 못 고르고 여러 날에 걸쳐 수렴합니다.
- **시간 초과 원인 확인**: 초과한 클러스터는 처리 대수(33대)가 많아서가 아니라
  **이동거리가 44km로 길어서**였습니다(최단 클러스터의 2.5배). 작업량은 25~34대로
  고른데 거리만 벌어졌습니다 — **거리 가중치 `γ` 조정이 답**이라는 분석이 실증됐습니다.
- **Pick 개선률이 낮음**(43~50% vs Drop 77~83%): drop 필요량이 pick 가능량보다 커서
  pick 쪽이 덜 처리되는 것으로 보입니다. 전체 재고 부족 신호일 수 있어 확인이 필요합니다.

## ✅ 완료 (2026-08-12, 버전 1.9.0) — 실데이터 첫 전체 실행

세션 내내 최대 공백이던 **실데이터 검증**을 완료했습니다. step0~step4 전 단계가
실제 데이터로 완주했고, API 키가 없어 한 번도 못 돌렸던 **step3(TMAP 지도)도 성공**했습니다.

| 항목 | 결과 |
| --- | --- |
| 원천 데이터 | 5,396,686행 / 1.54GB / 2025-01~2026-03 (15개월) |
| DB 적재 | 109초, 월별 분리 (겨울 27만 vs 여름 54만 — 계절차 2배) |
| 실행 결과 | 대여소 1,349곳 → 후보 310곳 → 작업 87곳 → 클러스터 10개 |
| 성과 | 총 301km, 최장 121분, 시간 예산 준수 90%, 평균 개선률 65% |
| 소요 시간 | api_to_info 3.8초 / raw_to_net 3.3초 / step1 **42초** / step3 7.7초 |

**step1이 42초**로 끝났습니다 — 5분 이상 걸릴 것으로 예상했는데, 작업 대상이 87곳이라
조정 반복이 빨리 수렴했습니다. 성능 개선 우선순위를 낮춰도 되겠습니다.

발견해서 고친 문제:

- 🔴 **BOM 인코딩** — 원천 CSV가 utf-8-sig인데 코드는 utf-8로 읽어 첫 컬럼명이 깨짐
- 🔴 **step4 route_summary도 처리대수 2배** — 1.8.1에서 vrp.py만 고쳤었음. 방문수도
  depot 복귀를 포함해 부풀려져 있었음
- 🟡 **테스트 격리** — 실데이터가 생기자 CSV 폴백이 진짜 파일을 찾아 테스트가 깨짐.
  웹 테스트가 빈 임시 폴더를 보도록 격리
- 🟢 **`--split-by-month`** 추가 — 계절이 다른 달을 섞지 않도록 월별 적재

## ✅ 완료 (2026-08-11, 버전 1.8.1) — 처리 대수 수정·시간 예산 점검

| 항목 | 내용 |
| --- | --- |
| 🔴 `bikes` 2배 계산 | pick·drop의 qty를 모두 더해 한 대를 두 번 셌다(ILP 72대 → 144로 기록). pick 기준으로 수정. 작업시간은 두 동작이 모두 드는 게 맞아 그대로 |
| 시간 예산 도입 | `TIME_BUDGET_MINUTES`(기본 120분). vrp 경고 + step4 준수율 + `/vehicles` 준수율 카드·초과 행 강조 |
| 측정으로 밝힌 것 | **소요시간의 70~80%가 이동**. 120분이면 약 23대가 한계. 개선하려면 작업량보다 클러스터의 지리적 밀집도(거리 가중치 `γ`)를 손대야 함 |
| 테스트 2개 추가 (전체 97개) | `bikes`가 ILP 계획과 일치하는지, 예산 초과가 화면에 표시되는지 |

## ✅ 완료 (2026-08-11, 버전 1.8.0) — 차량 로테이션과 형평성 기록

보유 21대를 하루 약 3회차에 나눠 투입하는 실제 운용 모델을 반영했습니다.
설계와 규칙은 [FLEET.md](../구현/FLEET.md).

| 항목 | 내용 |
| --- | --- |
| `VEHICLE_TOTAL` 미사용 문제 해소 | `VEHICLES_PER_ROUND`(회차당 상한)가 step1의 클러스터 수를 제한. 이제 차량 대수가 **결과가 아니라 제약** |
| `vehicle`·`vehicle_assignment` 테이블 | 차량 마스터(정비 시 `active=0`)와 회차별 배정·작업량 원장 |
| `db.assign_vehicles()` | 누적 소요시간이 적은 차량부터 뽑고, 그중 한가한 차량에 무거운 클러스터 배정. 별도 순번표 없이 누적 기록만으로 로테이션 성립 |
| `/vehicles` 화면 + API | 투입 현황·형평성(누적 시간 편차)·차량별 누적·회차별 이력 |
| 테스트 14개 추가 (전체 95개) | 로테이션 교대·전 차량 순환·형평성 수렴·멱등성·정비 차량 제외·차량 부족 시 실패 |

**함께 고친 기존 버그**: Pick 후보만 있고 Drop이 없는 시간대에서 클러스터 수가 0이 되어
K-Medoids가 크래시했습니다. 하루 여러 회차를 돌리면 실제로 생기는 상황이라
step1·step2·step4가 크래시 대신 건너뛰도록 고쳤습니다.

합성 데이터 생성기도 세 시간대에 방향이 다른 흐름을 만들도록 개선했습니다
(출근 → 낮 → 퇴근). 이전에는 05~10시에만 흐름이 있어 2·3회차에 작업이 없었습니다.

## ✅ 완료 (2026-08-10, 버전 1.7.0) — 대여이력 적재·step0 전환 (4단계)

원천 대여이력(60만 행 규모)을 DB에 적재하고, step0 두 스크립트가 CSV 대신 DB에서 읽습니다.

| 항목 | 내용 |
| --- | --- |
| `db.bulk_load_rentals()` | 청크 단위 적재. 적재 중 인덱스를 지웠다 복구, period 단위 멱등. 594,000행에 6.1초 |
| `tools/load_rentals.py` | 적재 CLI (`--status`로 기간별 현황) |
| `db.read_rental_source()` | DB 우선·CSV 폴백. **원본 한글 컬럼명으로 반환**해 기존 계산 로직을 손대지 않음. `columns=`로 필요한 것만 읽음 |
| step0 전환 | `raw_to_net.py`(4컬럼)·`api_to_info.py`(9컬럼)가 DB에서 읽음 |
| 테스트 9개 추가 (전체 80개) | 적재 멱등성·인덱스 복구·날짜 정규화·기간 격리 + **CSV와 DB 경로 산출물 완전 동일성** |

측정으로 드러난 것 — **DB가 항상 빠른 게 아닙니다.** 한 달치만 다루면 CSV가 최대 1.8배
빠르고, 3개월치 저장소에서 한 달만 읽을 때 DB가 2.45배 빠릅니다. 저장소가 필요한 양보다
클 때만 이득이며, 1년치를 병합해 두고 한 달씩 쓰는 실제 운용이 여기 해당합니다.

동일성 테스트가 실제 결함 하나를 잡았습니다 — `REAL` 컬럼이 정수인 `이용시간(분)`을
실수로 바꿔 산출물 dtype이 달라지던 문제(→ `NUMERIC` 친화도로 수정).

## ✅ 완료 (2026-08-10, 버전 1.6.0) — 웹 API의 DB 전환 (3단계)

웹 API가 파일 대신 DB를 읽습니다. **"최신"의 기준이 파일 수정시각에서 `run_label`로** 바뀌었고,
과거 실행분 조회가 처음으로 가능해졌습니다.

| 항목 | 내용 |
| --- | --- |
| `webapp/store.py` | 데이터 조회 계층. DB 우선, 비어 있으면 CSV 폴백. `catalog.py`는 파일(지도·다운로드) 담당으로 역할 분리 |
| `?run_label=` · `?duration=` | 산출물 API가 과거 실행분을 지정해 조회 |
| `/api/pipeline-runs` | DB 실행 이력 목록 (`/api/runs/{id}`는 웹 작업 상태로 개념이 달라 이름 분리) |
| `/api/route-summary` | 클러스터별 이동거리·운행시간 API 신설 |
| 응답 봉투 | `run_label`·`source`(db\|csv)·`count`·`rows` — 어느 실행분을 어디서 읽었는지 표시 |
| 대시보드 | 첫 화면에 DB 실행 이력 표 추가 |
| 테스트 9개 추가 (전체 71개) | 최신 조회, 과거 실행 조회, 404 처리, 이력 목록. CSV 폴백은 별도 확인 |
| 테스트 격리 보강 | 라우트 호출만으로 실제 DB가 생성되는 문제를 `conftest.py` autouse fixture로 차단 |

## ✅ 완료 (2026-08-10, 버전 1.5.0) — SQLite 이중 기록 (2단계)

파이프라인을 돌리면 이제 CSV와 DB에 **동시에** 기록됩니다. 별도 적재 명령이 필요 없습니다.

| 항목 | 내용 |
| --- | --- |
| 이중 기록 | 9개 단계가 각자의 테이블에 기록 (`station_stock`·`parking_lot`·`station_info`·`net_demand`·`rebalance_plan`·`pick_drop`·`ilp_plan`·`vrp_plan`·`metrics`·`route_summary`) |
| `db.save_output()` | 단계용 헬퍼. **CSV가 정본이므로 DB 실패는 경고만 남기고 파이프라인을 멈추지 않는다** — 대신 테스트가 누락을 잡는다 |
| `db.session()` | 연결을 열고 스키마를 보장한 뒤 **닫는** 컨텍스트 매니저 (sqlite3의 `with`는 연결을 닫지 않아 자원이 샌다) |
| `db.ensure_run()` | 단계마다 아는 정보가 달라(`period`만/`duration`만) `COALESCE`로 누적 갱신. 먼저 기록된 값을 덮어쓰지 않는다 |
| `PBR_DB_PATH` | DB 경로 재정의 환경변수. **테스트가 실제 `data/bike_system.db`를 오염시키지 않도록** 임시 파일을 가리킨다 |
| 테스트 11개 추가 (전체 62개) | 테이블별 적재 9개 + CSV 대조 + `runs` 등록. 이중 기록이 하나라도 빠지면 실패한다 |

## ✅ 완료 (2026-08-10, 버전 1.4.0) — SQLite 저장소 1단계

CSV 파일명에 박혀 있던 `{now}` 라벨을 **`run_label` 컬럼**으로 옮기는 기반을 만들었습니다.
step 스크립트는 아직 CSV를 쓰며, 이관은 [DB_PLAN.md](../구현/DB_PLAN.md) 2단계에서 진행합니다.

| 항목 | 내용 |
| --- | --- |
| `db.py` | `data/bike_system.db` 연결(WAL), 11개 테이블 스키마, `save_frame`/`load_frame`/`latest_label`/`record_run`. 라벨 생략 시 최신 실행분 반환 |
| `tools/csv_to_db.py` | 기존 CSV 산출물을 DB로 적재. `--list`로 실행 이력 확인 |
| `tests/test_db.py` (16개) | 멱등 저장, 실행·시간대 격리, 한글 컬럼 변환, VRP 방문 순서 보존, 실행 간 비교 쿼리 |
| 통합 검증 (1개) | 파이프라인이 **실제로 만든 CSV**를 적재해 스키마 적합성과 행 수 일치 확인 |
| 설계 변경 | SQLAlchemy를 쓰기로 했다가 표준 sqlite3로 변경 — pandas 계층 코드가 동일해 이점이 없었고, 교체 지점을 `db.connect()` 하나로 격리 (DB_PLAN.md에 근거 기록) |

## ✅ 완료 (2026-08-10, 버전 1.3.0) — 스모크 테스트·샘플 데이터

수정할 때마다 수동으로 확인하던 것을 자동화했습니다. `python -m pytest`로 34개가 20초에 돕니다.

| 항목 | 내용 |
| --- | --- |
| `tools/make_sample_data.py` | 타슈 CSV 스키마를 재현한 합성 데이터 생성기. API 키·실데이터 없이 파이프라인과 대시보드를 돌려볼 수 있다 (TODO의 "샘플 데이터 부재" 해소) |
| `tests/test_webapp.py` (17개) | 라우트 렌더링·경로 탈출 차단·허용 확장자·404 처리. Starlette 시그니처 변경 같은 회귀를 즉시 잡는다 |
| `tests/test_pipeline.py` (17개) | 합성 데이터로 step0→step1→step2→step4 실제 실행. 산출물 존재·스키마, ILP 공급 제약, VRP 시간 컬럼, 개선량 부호까지 검증 |
| 실데이터 안전성 | 실행마다 고유 라벨(`smoketest-{pid}`)을 써서 실데이터와 파일명이 겹치지 않고, 종료 시 해당 라벨 파일만 정리 |
| `pytest.ini` | `testpaths = tests` — `experiments/learning/pulp_test.py`가 pytest 기본 패턴에 걸려 수집되던 문제 차단 |
| `requirements-dev.txt` | pytest·httpx 분리 (런타임 의존성에 섞지 않음) |

## ✅ 완료 (2026-08-10, 버전 1.2.2) — 웹 대시보드 코드 리뷰 수정

| 항목 | 내용 |
| --- | --- |
| 🔴 작업 ID 충돌 | 초 단위 ID → 같은 초 재실행 시 기록·로그 덮어씀. 밀리초+일련번호로 수정 |
| 🟡 실행 중단 불가 | `cancel_job()` + 중단 버튼 추가. Windows `taskkill /T`로 프로세스 트리 종료 |
| 🟡 409 UX | 폼 요청에 JSON 오류 대신 안내 문구가 붙은 실행 화면 반환 |
| 🟡 미리보기 비효율 | 200행 보여주며 전체 `read_csv` → `nrows` + 개행 카운트 |
| 🟢 이력 무한 누적 | `runs.json` 최근 100건 유지 |
| 🟢 기타 | 조회 함수 락(RLock), Popen 실패 시 파일 누수 방지, favicon 204, meta refresh `<head>` 이동 |

안전 확인: CSV 미리보기 XSS(pandas `to_html` 이스케이프), 경로 탈출 차단은 기존대로 정상.

## ✅ 완료 (2026-08-10, 버전 1.2.1) — 실제 환경 검증

Python 3.14.7 / Windows 11에서 설치·구동을 검증하고, 그 과정에서 드러난 문제를 수정했습니다.

| 검증 항목 | 결과 |
| --- | --- |
| `pip install -r requirements.txt` | 구버전 고정 → **실패**(아래 수정) → 갱신 후 성공 |
| `py_compile` (27개 파일) | 통과 |
| 전체 모듈·step 스크립트 import (13개) | 통과 |
| `run_pipeline.py --dry-run` | 13단계 정상 출력, exit 0 |
| webapp 기동·라우트 | 구 시그니처로 500 → **수정 후 전부 정상** |
| 웹 폼 실행 → 로그 → 상태 추적 | 정상 (한글 인자 보존 확인) |
| **전체 파이프라인 E2E** (합성 데이터: 대여소 90곳·이력 14,000건) | step0~step2·step4 전부 exit 0 |
| ㄴ step0 (4단계) | 통과 — 주차대수 파싱·대여소 정보·순수요·재배치량 산출 |
| ㄴ step1 (클러스터링·지도) | 통과 — 39개 대여소 → 6클러스터, 4.5초, \|balance\| ≤ 2 |
| ㄴ step2 (ILP·VRP) | 통과 — CBC `Optimal`, VRP 계획 생성 |
| ㄴ step4 (지표·경로요약) | 통과 — 평균 개선률 75%, 총 192km / 최장 95분 |
| 산출물 있는 상태의 webapp | 지도 HTML·CSV 미리보기·GeoJSON API 전부 200 |
| API 키 필요 단계 (tashu_api, step3 TMAP) | ❌ 미검증 — 키 없음 |

수정한 내용:

- ~~requirements.txt의 2023년 고정 버전~~ → Python 3.14의 C 확장 ABI와 맞지 않아
  numpy/pandas/scipy/sklearn/folium/matplotlib/fastapi가 **전부 import 실패**했음.
  3.14 휠이 있는 버전으로 갱신하고 하한(`>=`) 고정으로 변경.
- ~~`scikit-learn-extra` (step1 K-Medoids)~~ → 아카이브된 프로젝트라 Python 3.12+ 휠이 없고
  소스 빌드에 MSVC C++ 빌드툴을 요구해 **설치 불가**. Rust 구현(FasterPAM)의
  `kmedoids` 패키지로 교체(API 호환).
- ~~webapp 전 페이지 500 오류~~ → Starlette 1.x가 구
  `TemplateResponse(name, {"request": ...})` 시그니처를 제거함.
  `TemplateResponse(request, name, {...})`로 6개 라우트 수정.

## ✅ 완료 (2026-08-07, 버전 1.2.0)

### P2·P3 — 이번 라운드에 수정된 항목

- ~~step3 depot 복귀 KeyError~~ → `action == 'return'` 행을 depot 정보로 처리.
  depot id·좌표·차량 용량은 `project_config`의 공통 상수(DEPOT_*, VEHICLE_CAPACITY)로 통일
  (기존: main.py `ST0000` vs vrp.py `ST0001` 불일치).
- ~~vrp.py pick/drop 노드 덮어쓰기~~ → 노드 키를 `(station_id, type)`으로 변경.
- ~~VRP 시간 계산 미구현~~ → 이동거리(km)·이동시간·작업시간(자전거 1대당 30초 가정)·누적시간
  컬럼을 VRP 결과에 추가. drop-only 잔여 시 depot 무한복귀 가능성도 방어 코드 추가.
- ~~TMAP 경유지 30개 제한 방어 없음~~ → `module.call_tmap_chunked()`가 구간 분할 호출,
  `merge_tmap_results()`가 경로·누적시간 병합.
- ~~step4 총 이동거리·운행시간 미구현~~ → `route_summary()`가 클러스터별
  방문수·처리대수·총이동거리·총이동/작업/소요시간을 집계해 `route_summary*.csv` 저장.
- ~~module.py 죽은 코드~~ → 주석 처리된 구버전 정규화 유틸 삭제.
- ~~테스트 정리~~ → `test/`·각 step의 `test.py`를 `experiments/`로 이동 (experiments/README.md 참고).
- ~~`.env.example` 추가~~ → API 키·PBR_* 환경변수 템플릿.
- ~~`adjust_module` docstring 오류~~ → 유클리드 → 맨해튼 거리로 정정.

## ✅ 완료 (2026-08-07, 버전 1.0.3)

### P1 — 파이프라인이 깨지는 문제 (전체 해소)

- ~~**`now` 값 3원화**~~ → 전 스크립트가 `project_config.get_runtime_config()` 사용.
  CLI 인자 → 환경변수(`PBR_NOW` 등) → 기본값 순으로 결정되며, 모든 단계가 같은 라벨 공유.
  경로도 `PROJECT_ROOT` 기준 절대경로가 되어 어느 cwd에서 실행해도 동작.
- ~~**import 부작용 체인**~~ → step0의 상호 import 제거, 실행 로직을
  `if __name__ == '__main__':` + `main()`으로 격리. 실행 순서는 run_pipeline.py가 제어.
- ~~**EDA.py 즉시 크래시**~~ → `pd.DateFrame` 오타 2곳, `month_graph(file_path)` 인자 오류 수정.
  원천 파일이 없으면 정상 종료(파이프라인 중단 방지).
- ~~**run_pipeline 인자 무시**~~ → 하위 스크립트가 project_config로 인자를 읽으므로
  `--now/--period/--duration/--raw-file` 전달이 실제로 동작. docstring 파일명도 수정.
  `ensure_output_dirs()`를 실행 시작 시 호출.
- ~~**`!calculate_target_qty.py` 파일명**~~ → `calculate_target_qty.py`로 개명.

### P2 — 함께 수정된 항목

- ~~`tashu_api.py` API 실패 시 NameError·거짓 성공 메시지~~ → 실패 시 `SystemExit`, timeout 추가.
- ~~`calculate_target_qty.py`의 `.loc[:, ...].clip(inplace=True)`~~ → 재할당 방식으로 수정
  (pandas 2.x에서 target_qty 상·하한이 무시될 위험 제거).
- ~~`raw_to_net.py` 파일명 슬라이싱 period 추출~~ → `config.period` 사용.
- ~~`vrp.py` depot 복귀 기록의 `'from_id': sid`~~ → `current_id`로 수정, 복귀 후 `current_id` 갱신 추가.
- ~~`vrp.py` 죽은 코드~~ → `manhattan_distance_km`, `drop_qty` get 잔재 제거,
  station_info 로드를 클러스터 루프 밖으로 이동.
- ~~`ilp.py` solver 전역 의존~~ → `run_ilp_plan(metrics, duration, solver)` 인자로 변경.
  `assume_initial_stock` 미사용 상수 제거.
- ~~`step3/main.py` 전역 의존~~ → `make_vrp_map(..., duration, headers, tmap_url)` 인자화,
  duration_list 루프 지원.
- ~~`imbalance.py` 산출물 미저장~~ → verification CSV 저장·지도 생성 활성화,
  `title=` → `tiles=` 오타 수정, `fillna` no-op 제거.
- ~~`st_visualization.py` 유효하지 않은 `title=` kwarg~~ → 제거.
- ~~`concat_1year_file.py` 주석 토글 실행~~ → `--concat`/`--preprocess` CLI 옵션화,
  파일 없으면 건너뜀.

### 검증 상태

- ✅ 2026-08-10 Python 3.14.7 환경에서 검증 완료 (아래 1.2.1 섹션 참고).
